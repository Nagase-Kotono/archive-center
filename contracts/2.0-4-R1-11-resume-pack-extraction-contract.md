# 2.0-4.R1-11 Python Reference Extraction Contract — Resume Pack Slice

> Historical Archive Center 2.0 R0/R1 contract/evidence, reviewed 2026-09-08.
> “Current”, “planned” and route/schema counts below refer to that original snapshot.
> For active Go/Host ownership and implemented routes, use [STRUCTURE](../STRUCTURE.md)
> and [AI_GUARDRAILS](../AI_GUARDRAILS.md), checked against active source.
> This notice preserves the original contract; it does not declare a new API or schema.

> Status: **R1 recovery artifact** (2.0-side only; no 0.8 files modified)  
> Date: 2026-05-23  
> Depends on: `2.0-prep-readiness.md` (R0/R1 foundation complete)  
> Cross-reference: `_archive/future-reference/2.0 backend seperated.md` Phase 1-C

---

## 1. Chosen Slice

**Slice name:** `resume_pack` (Phase 1-C)

**Why this slice is safe:**
- Single entry point (`_bundle_resume_pack`) with one nested helper (`_clean_text`).
- Strictly read-only: performs three `SELECT` queries via SQLAlchemy ORM and returns a computed dict.
- Zero side effects: no DB writes, no LLM calls, no background tasks, no file I/O.
- Zero coupling to `palace/bridge.py`: all queries are inline ORM calls using `SessionLocal()`.
- No imports from `backend.main` required if extracted; dependencies are limited to `SessionLocal`, ORM models (`SagaDigest`, `ArcSummary`, `ChapterSummary`), and `to_kst`.
- Exact current source block is 123 lines in 0.8 (`backend/main.py:3472-3594`), making it the smallest currently accepted Phase 1 candidate.

---

## 2. Exact 0.8 Source References

### 2.1 `backend/main.py`

**File metrics (read-only):**
- Total lines: **23,723**
- Total top-level `def` count: **406**
- Total nested-inclusive `def` count: **442**

**Functions referenced:**

| Function | Location | Lines | Kind |
|----------|----------|-------|------|
| `_bundle_resume_pack` | `backend/main.py:3472` | 3472–3594 | resume main |
| `_clean_text` (nested) | `backend/main.py:3520` | 3520–3524 | text util |

**Behavioral summary of `_bundle_resume_pack`:**
- Accepts `sid: str` and `continuity_trigger_mode: str`.
- Normalizes trigger mode to one of `resume`, `idle`, `empty`, or returns `None`.
- Queries the latest `SagaDigest`, `ArcSummary`, and `ChapterSummary` rows for the session, ordered by `to_turn` descending.
- Builds a hierarchy of cleaned text sections using nested `_clean_text` (truncates with `"..."` at configurable `max_len`).
- Returns a dict with keys: `pack_status`, `trigger`, `sources_used`, `layer_count`, `assembled_text`, `saga`, `arc`, `chapter`, `assembly_note`.

**Dependencies used inside the block (all already available in 2.0 store layer or shared utils):**
- `SessionLocal` (SQLAlchemy session factory)
- `SagaDigest`, `ArcSummary`, `ChapterSummary` (ORM models)
- `to_kst` (datetime helper)
- `logger` (standard logging)

### 2.2 `backend/palace/bridge.py`

**File metrics (read-only):**
- Total lines: **9,419**
- Total top-level `def` count: **149**
- Total nested-inclusive `def` count: **170**

**Functions referenced:** None. `_bundle_resume_pack` does **not** call any `palace/bridge.py` functions.

**Note:** The 0.8 `bridge.py` *does* contain read-only functions such as `get_saga_digests_for_session`, `get_arc_summaries_for_session`, and `get_chapter_summaries_for_session`, but `_bundle_resume_pack` bypasses them and issues its own `ORDER BY ... DESC` queries. This makes the slice internally consistent and avoids bridge.py entanglement.

---

## 3. Target 2.0 Owner / Module / Route Pairing

### Recommended pairing

| Aspect | Recommendation |
|--------|----------------|
| **Go group file** | `go-service/internal/httpapi/group_narrative.go` (existing) or a new `group_session.go` if the team splits session routes later. |
| **Route** | `GET /sessions/{chat_session_id}/resume-pack` |
| **Handler name** | `handleSessionResumePack` |
| **Store method** | Add to `go-service/internal/store` (e.g., `GetLatestSagaDigest`, `GetLatestArcSummary`, `GetLatestChapterSummary`) or reuse existing equivalents if already present. |
| **DTO** | Define a `ResumePack` struct in `go-service/internal/store` or `go-service/internal/httpapi/dto` matching the Python return shape exactly. |

### Existing 2.0 proximity

The Go codebase already has a placeholder `GET /continuity-pack/{chat_session_id}` (`handleContinuityPack`) in `group_narrative.go`. The `resume-pack` concept is adjacent but distinct:
- `continuity-pack` is a broader narrative-state snapshot.
- `resume-pack` is the specific P-4c hierarchy summary used for long-gap re-entry.

**Decision:** Keep `resume-pack` as a separate route so the compare harness can shadow parity-test it independently. If the team prefers consolidation, the acceptance criteria below still apply to whichever route owns the payload.

---

## 4. What Must NOT Be Implemented Yet

| Item | Reason |
|------|--------|
| DB writes / mutations | This slice is read-only; write shadow is R2. |
| Injection / `input_context` wiring | The Python docstring explicitly states this pack is "not wired into injection or input_context." Do not connect it to turn pipeline injection in Go. |
| LLM calls / prompt templates | No generation happens in this slice. |
| `palace/bridge.py` extraction | Phase 1-D is a separate track; do not mix it here. |
| Chroma shadow / vector backfill | Blocked by live-read switch gate (R2). |
| Session migration / authority switch | Blocked by authority switch gate (R2). |

---

## 5. Acceptance Criteria for Future Go/Python Parity Work

1. **Route exists and compiles.** Go route `GET /sessions/{chat_session_id}/resume-pack` is registered and returns HTTP 200.
2. **Field parity.** Response JSON contains exactly these top-level fields:
   - `pack_status` (`"ready"` or `"empty"`)
   - `trigger` (`"resume" | "idle" | "empty"`)
   - `sources_used` (`["saga","arc","chapter"]` etc.)
   - `layer_count` (integer)
   - `assembled_text` (string or `""`)
   - `saga` (object or `null`)
   - `arc` (object or `null`)
   - `chapter` (object or `null`)
   - `assembly_note` (string)
3. **Nested object parity.** `saga`, `arc`, and `chapter` objects contain the same field names as the Python payload (e.g., `id`, `from_turn`, `to_turn`, `era_label`, `resume_pack_text`, `saga_summary`, `arc_index`, `arc_name`, `arc_status`, `arc_resume_text`, `core_conflict`, `chapter_index`, `chapter_title`, `resume_text`, `summary_text`, `created_at`).
4. **`_clean_text` behavior match.** Text helper truncates to `max_len - 3` and appends `"..."`; whitespace is collapsed via `" ".join(str(raw or "").split()).strip()`.
5. **Trigger normalization match.** `"resume"` and `"manual_resume"` → `"resume"`; `"idle"` and `"idle_reentry"` → `"idle"`; `"empty"` and `"empty_input"` → `"empty"`; everything else → `null`/HTTP 204 or empty body.
6. **Ordering match.** SQL queries order by `to_turn DESC`, then `id DESC`, and take `.first()`.
7. **Null-safety match.** Missing optional fields on ORM rows must not panic; they should serialize as `null` or zero values consistently with Python.
8. **Compare harness evidence.** `internal/compare/harness.go` (or equivalent) produces a parity report showing this route’s JSON schema and sample output matching the Python reference within the allowed variance list.

---

## 6. Verification Statement

**No file under the read-only `<legacy-source-root>` was created, deleted, or edited during the production of this artifact.**
All 0.8 inspection was performed via read-only `Get-Content` and `Select-String` operations.
All edits were confined to the active Archive Center contract document.

---

*Document version: R1-2026-05-23*  
*Cross-reference: `2.0 backend seperated.md` Phase 1-C, `go-route-group-design.md`, `group_narrative.go`*
