# 2.0 Risk Ledger

> Historical Archive Center 2.0 R0/R1 contract/evidence, reviewed 2026-09-08.
> “Current”, “planned” and route/schema counts below refer to that original snapshot.
> For active Go/Host ownership and implemented routes, use [STRUCTURE](../STRUCTURE.md)
> and [AI_GUARDRAILS](../AI_GUARDRAILS.md), checked against active source.
> This notice preserves the original contract; it does not declare a new API or schema.

> Status: R0 initial inventory. Not exhaustive. Not a release gate.
> Date: 2026-05-21
> Policy: Do not claim green. Every item needs an owner and evidence before R2.

## Classification

| Tag | Meaning |
|-----|---------|
| fix-now | Must be resolved before any shadow traffic is accepted. |
| migration-owner | Expected to be resolved during R1 parity work by the assigned owner. |
| defer-with-evidence | Acceptable to defer, but only with a written record of the trade-off and acceptance criteria. |

---

## Category: Route / Security

| # | Risk | Classification | Owner | Evidence / Mitigation |
|---|------|----------------|-------|---------------------|
| R1 | 132 routes in 0.8 backend; only 3 trivial endpoints exist in Go skeleton. | migration-owner | @kimi_coder | Route inventory exists in `contracts/current-route-inventory.md`. |
| R2 | No request/response JSON schemas extracted from Pydantic models yet. | migration-owner | @kimi_coder | Blocker noted in route inventory. Next step: schema extraction. |
| R3 | Safe probe boundary is currently hard-coded to `GET /health` only. | fix-now | @kimi_coder | CLI and `internal/bench` enforce this; any expansion requires explicit review. |
| R4 | Authentication/authorization envelope is undefined in Go skeleton. | defer-with-evidence | @deepseek_validator | 0.8 uses local-operator model; Go equivalent not yet designed. Documented as defer. |
| R5 | `IsLiveCutoverAllowed()` is hard-coded `false`; no feature-flag infra exists. | migration-owner | @kimi_coder | Explicit guard in config + handler layer. Future R2 needs flag system. |

## Category: Truth Migration (MariaDB)

| # | Risk | Classification | Owner | Evidence / Mitigation |
|---|------|----------------|-------|---------------------|
| T1 | MariaDB not connected in Go skeleton; readiness reports `not_configured`. | migration-owner | @kimi_coder | Expected in R0. Readiness contract documents this state. |
| T2 | No SQL schema migration from 0.8 SQLite → MariaDB exists yet. | migration-owner | @kimi_coder | `migrations/` directory is empty placeholder. |
| T3 | SQLAlchemy session lifecycle semantics (flush, expire, refresh) differ from `database/sql`. | defer-with-evidence | @deepseek_validator | Risk acknowledged. Must be tested with shadow write/read/compare before R2. |
| T4 | Truth authority cutover is a single R2 event; it must not happen before parity replay passes. | fix-now | @kimi_coder | Blocked by policy in `substrate-boundary.md` and `go-shadow-api.md`. |

## Category: Vector Runtime (ChromaDB)

| # | Risk | Classification | Owner | Evidence / Mitigation |
|---|------|----------------|-------|---------------------|
| V1 | ChromaDB configuration, collection dimension, and API compatibility can drift across installations. | migration-owner | @kimi_coder | Pin the packaged ChromaDB version and fail readiness with a clear compatibility error. |
| V2 | Chroma retrieval has complex provenance tracking. | defer-with-evidence | @deepseek_validator | Preserve provenance and query-sensitive ranking in regression tests. |
| V3 | Vector lane must support degraded fail-open: relational truth usable when vector is stale. | migration-owner | @kimi_coder | Explicit requirement in `substrate-boundary.md`. Need test in R1. |
| V4 | Changing embedding models can conflict with an existing collection dimension. | fix-now | @kimi_coder | Detect the mismatch before reindex and require an explicit collection rebuild. |

## Category: Go Runtime

| # | Risk | Classification | Owner | Evidence / Mitigation |
|---|------|----------------|-------|---------------------|
| G1 | Go toolchain was not available during initial skeleton write; files had BOM/format issues. | fix-now | @kimi_coder | **Resolved**: BOM removed, `gofmt` applied, `go test` and `go vet` pass. |
| G2 | No structured logging adapter for `slog` beyond JSON stderr. | defer-with-evidence | @glm_subcoder | Acceptable for R0. Need log level control and rotation before R2. |
| G3 | Build metadata (`BuildVersion`, `BuildCommit`) defaults to static strings. | defer-with-evidence | @glm_subcoder | Should be injected at compile time via `-ldflags` in R1. |
| G4 | No graceful shutdown (no `http.Server` with `Shutdown`). | migration-owner | @kimi_coder | Current `http.ListenAndServe` is acceptable for R0 shadow. Must add before R2. |

## Category: Artifact Hygiene

| # | Risk | Classification | Owner | Evidence / Mitigation |
|---|------|----------------|-------|---------------------|
| H1 | `.gocache` was created inside the workspace during first Go tool runs. | fix-now | @kimi_coder | **Resolved**: cleaned after validation. Future tool runs should use a temp cache. |
| H2 | Accidental creation of `.env`, DB, or vector artifacts in 2.0 workspace. | fix-now | @kimi_coder | Policy enforced by `AGENTS.md` instructions and manual verification. |
| H3 | No CI gate to prevent forbidden artifacts from being committed. | defer-with-evidence | @deepseek_validator | Not required for R0. Recommended before R1 parity work begins. |

## Category: Benchmark / Tooling

| # | Risk | Classification | Owner | Evidence / Mitigation |
|---|------|----------------|-------|---------------------|
| B1 | Baseline capture CLI only probes `GET /health`; no meaningful backend work is measured. | migration-owner | @kimi_coder | By design for R0. Must expand safe paths after review in R1. |
| B2 | Percentile calculation uses linear interpolation; small sample sizes may be noisy. | defer-with-evidence | @kimi_coder | Documented in `current-runtime-baseline-capture.md`. Acceptable for early evidence. |
| B3 | Latency includes client overhead; not server-side-only. | defer-with-evidence | @kimi_coder | Documented limitation. Future slice may add server-side timing. |
| B4 | No automated benchmark regression gate exists. | defer-with-evidence | @deepseek_validator | Not required for R0. Should be added before R2 cutover. |

---

## Summary Counts

| Classification | Count |
|----------------|-------|
| fix-now | 6 |
| migration-owner | 9 |
| defer-with-evidence | 9 |

## Next Actions

1. Resolve remaining `fix-now` items (T4, V4, H1, H2 already addressed in this slice; R3 remains active: safe-probe boundary is still `GET /health` only).
2. Begin JSON schema extraction from 0.8 Pydantic models to unblock route parity shadow.
3. Safe-probe list expansion is deferred-with-evidence; the boundary remains `GET /health` only until explicit review in R1.
