# Archive Center 3.7 — 3.5–3.6 전체 버그 감사 및 해결 계획

상태: **3.7 기능·소스 기준선 고정 / Windows 테스트 빌드 핵심 RisuAI 수명주기 검증 완료 / 3.7-G 전체 환경 행렬은 후속 증거로 유지**

작성일: 2026-07-30 KST

감사 기준 커밋: `d167f82` (`fix: enable Windows 3.6 managed vector runtime`)

감사 대상 빌드 표기: `3.6.0-dev / 3.6-precision-memory.20260729-3`

대상 소스: `Archive Center.js`, `go-service`, Windows 관리형 패키지와 업데이트 경로

구현 목표 버전: **3.7**

배포 판정: **아래 차단 조건이 닫히기 전까지 3.7 실 배포 금지**

현재 판정:

- 기존의 `3.7-A~F source 완료` 판정은 철회한다. 문서의 요구사항·배포 차단
  조건과 production 호출 경로를 일대일로 대조하지 않고 기존 커밋·테스트 이름을
  완료 증거로 재사용했다.
- 2026-07-30 재감사에서 확인한 JavaScript rollback timer·Reference/Admin
  job polling·후처리 출력 재확인, Go memory worker ticker와 Vector outbox
  retry, CLI·launcher readiness sleep은 제거하거나 사용자 UI/호출자 context에
  연결했다. 이 수정은 통합 회귀와 정적 감사를 모두 통과해야 소스 완료로 판정한다.
- `700ms·800ms·8초·90초·12초`는 finality에서 전부 제거했다. 설정 화면의
  request/LLM timeout과 사용자가 선택하는 재시도 횟수는 숨은 finality 정책이
  아니므로 유지한다. 특히 UI의 `criticTimeout=90`은 평론가 호출 timeout이며
  과거의 `감시 강제 종료 90초`와 다른 값이다.
- A~F의 각 항목은 production entrypoint와 그 경로를 실행하는 회귀 테스트가
  확인된 뒤에만 다시 완료로 바꾼다. source 문자열 검사나 문서 기록만으로
  완료 처리하지 않는다.
- 플러그인·Go·Windows/POSIX package builder와 배포용 env 예시의 기본 표기는
  `3.7.0-dev`로 동기화했다. 실제 사용자 `.env`와 DB는 패키지에 포함하지 않는다.
- package file hash는 생성되지만 source commit → build command → package manifest →
  loaded artifact hash 연결은 아직 없다. 3.7-G의 첫 진입 작업으로 닫는다.
- 실제 RisuAI, 신규·기존 Windows 설치, packaged MariaDB/Chroma, 실제 provider와
  updater health commit/rollback은 아직 검증하지 않았다.

### 2026-07-30 통합 소스 검증 기록

- bundled Node로 `Archive Center.js --check` 통과
- workspace-local Go cache/temp를 사용한 `go test ./... -count=1 -timeout 15m`
  전체 통과
- `go vet ./...` 통과
- `internal/httpapi` source acceptance, HUD event stream order/terminal/cancel,
  listener cleanup과 `cmd/js-route-variant-smoke` 전용 회귀 통과
- 변경된 PowerShell 실행기 9개의 parser 검사 통과
- Git for Windows `bash -n`으로 POSIX launcher 구문 검사 통과
- production 경로 정적 감사에서 `setInterval`, 숫자 literal 기반
  `Start-Sleep`/`sleep`, `time.Sleep`/`time.After`/`NewTicker`,
  `context.WithTimeout`, 숫자 literal `setTimeout` 0건
- JavaScript의 유일한 `setTimeout`은 설정 UI의 `requestTimeoutMs`에서 전달된
  transport timeout을 사용한다. HUD 경과 초 표시는 `requestAnimationFrame`이며
  finality 판정이나 backend 재조회에 사용하지 않는다.
- 자동 HUD `/turn-workflow/status` 재조회는 0건이며
  `/turn-workflow/events` 단일 NDJSON 연결만 사용한다.
- 기준 HEAD는 `e070d6a`이고 working tree 수정본으로 빌드했다. 따라서 아래
  테스트 artifact는 GitHub release나 실배포 증거가 아니라 로컬 검증용이다.

### 2026-07-30 콜드 스타트·큐·숨은 처리 상한 재검증

이 절은 위의 과거 검증 기록 뒤에 발견된 회귀와 현재 working tree의 수정 상태를
기록한다. 과거 ZIP artifact와 해시는 이 수정본의 증거가 아니다.

- Session Normalize는 RisuAI 화면의 과거 메시지를 새 `active_final`처럼 소급
  합성하지 않는다. raw repair가 끝난 뒤 backend DB에 존재하는 전체 완전한
  user/assistant 원문 쌍을 canonical cold-start source로 사용한다.
- canonical source revision은 `session + turn + 원문 hash`로 결정되며 현재 시각,
  처리 순서, 세션 길이를 identity에 넣지 않는다. 같은 원문은 idempotent이고,
  기존 active source와 DB 원문이 다르면 `active_source_raw_mismatch`로 Critic
  호출 전에 차단한다.
- Critic과 파생 저장은 live durable worker와 동일한 source-fenced 처리 owner를
  사용한다. 실제 저장과 source revision 재확인이 끝나기 전에는 succeeded로
  집계하지 않는다. retryable 실패만 durable queue로 넘기고 terminal 실패는
  성공이나 대기로 바꾸지 않는다.
- derived 재처리가 failed/deferred/queued 상태이면 reindex를 성공처럼 실행하지 않고
  `derived_reprocessing_incomplete`로 연기한다. queued는 succeeded로 세지 않는다.
- Session Normalize, reindex, hierarchy 조회에서 `0=전체` 계약을 사용하고
  25/200/1000/5000 같은 숨은 전체 처리 상한을 제거했다. 페이지 크기는 전체
  처리 상한이 아니며 다음 cursor/offset이 있는 동안 계속 읽는다.
- JavaScript transport 실패 큐는 더 이상 RisuAI host 신호 한 번에 한 항목만
  처리하지 않는다. 해당 신호 시점에 있던 retryable 항목을 각각 정확히 한 번씩
  순차 처리하며, timer·polling·즉시 재시도는 사용하지 않는다.
- rollback과 session delete의 Vector outbox inline drain에서 64/128 고정 상한을
  제거했다. `limit=0`은 현재 wake에서 처리 가능한 전체 항목을 뜻하고, 같은 outbox
  ID가 다시 반환되면 반복 처리를 중단한다.
- Vector retry cursor는 같은 wake의 retry item을 즉시 다시 claim하지 않도록
  exclusive 기준을 사용하면서, 뒤의 다른 eligible item은 계속 처리한다.
- current OOC guard는 이전 대화 context의 OOC 문구 때문에 현재 정상 턴을
  취소하지 않도록 현재 user/assistant만 검사한다.
- Critic current user/assistant 입력의 4000/9000 문자 절단과 별도 world-audit
  token/temperature 덮어쓰기를 제거하고, 사용자가 설정한 provider 계약을 따른다.

현재 자동 검증:

- bundled Node `Archive Center.js --check`: 통과
- `go test ./... -count=1`: 통과
- canonical raw cold-start replay, raw/source mismatch 차단, OOC 제외, derived
  재처리 실패·연기, unbounded reindex/hierarchy, Vector retry/outbox, failed queue
  multi-item fixture: 통과

남은 검증:

- 실제 RisuAI/PocketRisu에서 cold start, reroll, delete, branch/copy를 수행한
  loaded-plugin 증거
- 실제 MariaDB 동시 transaction 경합과 packaged MariaDB/Chroma 설치·readback
- 실제 provider 호출과 updater health commit/rollback

테스트 배포 방식은 이 수정본부터 ZIP과 날짜별 폴더 생성을 중단한다. 실행기를
포함한 고정 Windows 테스트 폴더 하나만 유지하며, source 자동 검증을 통과한
working tree를 같은 경로에 제자리 갱신한다. 실제 `.env`, DB, log, cache, 사용자
data는 그 폴더에 복사하지 않는다.

- 고정 경로: `_dist-test-3.7-live/Archive Center 3.7 Windows Test`
- 실행기: `01_start_archive_center_windows.bat`
- 현재 생성 결과: managed/full_local, ChromaDB managed install, manifest
  `status=green`, ZIP 0개, 금지된 `.env`·DB·log·cache·data 0개
- package의 `Archive Center.js`는 source와 기능 코드가 같고 package version
  표기만 `3.7.0-dev`에서 `3.7.0-test`로 치환된다.

### 2026-07-30 Session Normalize 콜드 스타트 진행 표시 회귀

재현 증상:

- `Session Normalize Background Job`이 `raw_repair_replay / 8% (0/0)`에
  머물러 콜드 스타트가 진행되지 않는 것처럼 보였다.

확정 원인:

- 고정 주기 Admin job polling을 제거한 뒤 `Archive Center.js`의 상태 조회가
  한 번의 GET으로 끝났고, 이후 backend 상태 변경을 전달받는 경로가 없었다.
- `raw_repair_replay`는 `repair_entry_count`만 보냈지만 공용 progress renderer는
  `candidate_count`와 `processed`를 읽어 실제 항목이 있어도 `0/0`으로 표시했다.

수정 계약:

- Go `adminJobManager`가 단조 증가 `revision`과 `terminal`을 소유한다.
- `/admin/jobs/{job_id}/events`는 상태가 실제로 변경될 때만 한 NDJSON 연결로
  snapshot을 전달하며 terminal snapshot 뒤 종료한다.
- JavaScript는 해당 stream을 수신해 표시만 갱신한다. 고정 interval, 자동 polling
  fallback, 재귀 재조회는 사용하지 않는다. stream 미지원 때만 typed 알림과
  사용자가 누르는 수동 Refresh를 남긴다.
- raw repair는 시작 시 `0/N`, 각 항목 처리 뒤
  `processed/succeeded/failed/skipped/last_processed`를 갱신한다.
- 동일 작업 교체, terminal, 취소, plugin unload 때 stream reader와 controller를
  정리한다.

검증 범위:

- Go event stream revision 순서·terminal 종료와 raw repair `0/N → N/N` 회귀
  테스트를 추가했다.
- JavaScript 단일 연결·순차 snapshot·고정 timer 부재·terminal/교체/unload cleanup
  fixture를 추가했다.
- 이 기록은 source 검증이며 실제 RisuAI에 다시 로드한 테스트 빌드의 콜드 스타트
  증거는 3.7-G에서 별도로 확보해야 한다.

새 로컬 검증 artifact:

- version: `3.7.0-test.20260730-session-normalize-1`
- Windows Auto Install ZIP SHA-256:
  `EA2996BD08F416264A25E3577D552BF530C853A7F6832D38DA0C22C7753104DF`
- Windows Update ZIP SHA-256:
  `CEDA20FC14E8B17B4423217346FD46DC7348940A79F42677193D263C018EDC86`
- Compatibility Bridge ZIP SHA-256:
  `56A93EBB480926F5B84829E49067C151D5FC360CBC5111F70B4CAFCA658B5FCB`
- 세 ZIP의 실제 `.env`, DB/SQLite, data/log/cache 일치 항목은 0건이다.
- Auto Install staging의 명시적 caller timeout 기반 fresh-install smoke는
  `shadow/noop` backend ready와 package contract를 통과했다. 실제 MariaDB·Chroma
  다운로드·설치와 RisuAI 콜드 스타트는 아직 실환경 검증 항목이다.

Windows 관리형 테스트 artifact:

- ZIP: `_dist-test-3.7-finality-no-hidden-time/Archive Center 3.7.0 Windows Test Build.zip`
- version: `3.7.0-test.20260730`
- SHA-256:
  `e43f6db9cb1fc51111f2b28c1023c43be8aa5f8ab486af20b54bdca278aa8320`
- ZIP 45개 entry 검사에서 실제 `.env`, `.env.full.local`,
  `.env.full.local.protected`, DB/SQLite 파일, log, cache, 사용자 data 0건
- 실제 설치·RisuAI 로드·MariaDB/Chroma roundtrip은 여전히 3.7-G gate다.

버전 이동 원칙:

- 이미 구현된 3.6-A~F와 3.6 소스 이력은 3.6으로 유지한다.
- 이 문서의 전체 안정화 작업을 새 3.7로 삽입한다.
- 기존 미래 계획은 `3.7→3.8`, `3.8→3.9`, `3.9→4.0`,
  `4.0→4.1`로 한 단계 이동한다.

이 문서는 여러 사용자가 보고한 다음 현상을 하나도 누락하지 않고 다시 분류하기
위해 작성했다.

- `waiting for RisuAI active chat confirmation`이 끝나지 않는 현상
- DeepSeek에서 멈추고 GLM에서는 정상인 현상
- 700ms, 800ms, 8초, 90초, 12초 등 UI와 무관한 고정 시간
- 실패 큐가 30분 이상 남고 시도 횟수가 수십·수백 회가 되는 현상
- `LOGICAL_TURN_REPLACE_FAILED`
- `complete-turn verify_unavailable`, `raw_chat_log_verify_unavailable`
- `CRITIC_LLM_FAILED`, `critic_extract_failed`, JSON 생성·해석 실패
- 평론가가 만든 기억이 주입되지 않거나 보호 기억이 과도하게 생기는 현상
- `maintenance_enqueued`, `drift_detected`, `repair_replay`,
  `session_normalize`, `admin_reindex` 로그가 실제 무엇을 뜻하는지 불명확한 문제
- 새 세션에서도 반복되는 `vector_not_configured`
- Windows ChromaDB 설치·기동·연결 불일치
- MariaDB 기존 설치·업데이트 환경의 권한 문제
- 3.0에서 3.5로 올라갈 때 updater가 `status`를 반환하지 않는 현상
- Memory Explorer의 턴 수와 현재 RisuAI 턴 수가 다른 현상
- 세션 이관, 캐릭터 인덱스 변경, pin, 수동 attach의 불안정성
- 서사 가이드가 예산 부족으로 계속 빠지는 현상
- 입력 조립 투명성에 실제 전달값이 아닌 후보값이 표시되는 현상
- OOC, 삭제, 리롤, 중복 감지 HUD의 상태와 닫기 규칙
- prefill이 실제 사용자 입력으로 오인된다는 제보와 반복 서사 제보

## 1. 판정 원칙

이 문서의 상태 표기는 다음과 같다.

| 판정 | 의미 |
|---|---|
| 확정 결함 | 현재 소스에서 잘못된 경로 또는 잘못된 계약을 직접 확인했다. |
| 구현됨·실환경 미검증 | 수정 소스와 회귀 테스트는 있으나 실제 RisuAI, MariaDB, ChromaDB 또는 공급자 호출 증거가 없다. |
| 원인 일부 확정 | 실패 지점은 찾았지만 사용자 환경의 실제 하위 오류는 응답 본문·DB 오류·원본 trace가 없어 단정할 수 없다. |
| 재현 필요 | 제보는 있으나 현재 로그와 소스만으로 동일 현상을 재현하거나 원인을 연결하지 못했다. |
| 별도 문제 | 다른 현상과 같은 화면에 나타났지만 인과관계가 없다. |

소스 테스트, 문자열 smoke test, cross-build는 실제 RisuAI에 로드된 플러그인,
실제 MariaDB/ChromaDB, 실제 공급자 응답을 증명하지 않는다. 이 구분을 배포 판정에
계속 유지한다.

## 2. 결론

보고된 현상은 하나의 원인도 아니고, 전부 서로 무관한 오류도 아니다. 크게 네 개의
원인군으로 묶인다.

1. **최종 출력 확정과 저장 상태 머신 결함**
   - `afterRequest` 이후 JavaScript가 시간을 재며 RisuAI 채팅을 반복 조회한다.
   - 후보 출력 확정, 저장, 재시도 큐가 서로 분리되지 않았다.
   - DeepSeek의 느리거나 변동이 긴 스트리밍은 이 결함을 더 쉽게 드러낼 뿐,
     모델 자체가 근본 원인은 아니다.

2. **파생 기억 LLM과 상태 표기 결함**
   - 공급자 오류, 빈 응답, JSON 부재, JSON 문법 오류가 모두
     `CRITIC_LLM_FAILED` 하나로 합쳐진다.
   - 저장은 성공했는데 선택적 read-back만 실패한 경우도 경고로 보인다.
   - 실제 worker가 없는데 audit 기록을 `maintenance_enqueued`라고 표시한다.

3. **세션·이관·Vector·업데이트 계약 결함**
   - 현 세션 이관은 전체 session-scoped 테이블을 복사하지 않으면서 원본 cleanup을
     허용한다. 이는 P0 데이터 손실 위험이다.
   - 세션 pin은 캐릭터/채팅 인덱스 기반이며 저장 실패가 상위로 전달되지 않는다.
   - Windows 관리형 ChromaDB는 현재 소스에서 보완됐지만 기존 3.5 사용자 및 실제
     업데이터 경로는 아직 실환경 검증되지 않았다.

4. **진단 UI의 사실성 결함**
   - 후보(pre-budget), 선택(selected), 실제 전달(delivered)이 섞여 보인다.
   - 같은 `경고` 색상에 정보성 상태와 실제 장애가 함께 나타난다.
   - 일부 updater UI 경로에는 현재도 정의되지 않은 `$` 호출이 남아 있다.

따라서 `timeout만 늘리기`, `DeepSeek만 예외 처리하기`, `실패 큐 횟수만 더 늘리기`,
`JSON repair를 더 공격적으로 만들기`는 해결책이 아니다.

## 3. 전체 피드백 대응표

| # | 사용자 증상 또는 질문 | 재감사 판정 | 실제 의미 | 필요한 조치 |
|---:|---|---|---|---|
| 1 | `waiting for RisuAI active chat confirmation` | **확정 결함** | JavaScript가 최종성 정책을 소유하고 폴링·시간 안정성으로 확정한다. | 후보 관측과 backend acceptance를 분리하고 고정 시간 기반 최종성 판단을 제거한다. |
| 2 | DeepSeek는 멈추고 GLM은 정상 | **원인 일부 확정** | 모델별 스트리밍 패턴·지연 차이가 결함을 노출한다. | 모델명 분기 없이 동일 host lifecycle 계약으로 처리한다. |
| 3 | 700/800ms, 8/90/12초 고정값 | **확정 결함** | UI 설정과 무관한 숨은 최종성 정책이다. | RisuAI 신호로만 관측을 깨우고, transport timeout만 사용자 설정을 사용한다. |
| 4 | 실패 큐가 30분·수백 번 유지 | **확정 결함** | 일부 `continue` 경로가 최대 시도 횟수 검사를 건너뛴다. | pending confirmation과 transport retry를 별도 상태로 분리한다. |
| 5 | `LOGICAL_TURN_REPLACE_FAILED` | **실패 지점 확정, 하위 원인 미확정** | canonical tail 교체 API 또는 DB transaction이 실패했다. | backend typed error와 HTTP 응답 본문을 HUD·debug에 보존한다. |
| 6 | `raw:ok`, `derived:ok`, `verify_unavailable` | **표기 결함** | 저장 성공 후 선택적 canonical read-back이 실패했다. | 성공을 유지하고 read-back 미확인은 정보 알림으로 내린다. |
| 7 | `CRITIC_LLM_FAILED` | **분류 결함** | upstream·timeout·empty·parse·schema 실패가 합쳐졌다. | 단계별 안정 코드로 분리한다. |
| 8 | `invalid character ...`, `critic_json_missing` | **3.6 일부 보완·잔여 결함** | raw-first parser와 Vertex JSON MIME은 적용됐지만 실패 분류가 없다. | `raw_preview` 기반 원인 분류와 provider fixture를 추가한다. |
| 9 | OpenAI 형식에서는 정상, Vertex 형식에서는 실패 | **공급자 adapter 가능성 높음** | 모델 자체보다 응답 추출·구조화 출력 계약 차이와 일치한다. | 같은 prompt의 provider별 raw response fixture와 content extraction을 비교한다. |
| 10 | 평론가가 만든 내용이 주입되지 않음 | **복수 원인 가능** | critic 실패, revision stale, budget defer, worker retry 중 하나일 수 있다. | 저장/파생/admission/vector/delivery 상태를 한 lineage로 노출한다. |
| 11 | 보호 기억이 지나치게 많이 붙음 | **guard 원리 확정, 오염 원인 재검증 필요** | policy 없는 protected item은 fail-closed guard가 된다. `belief_updates` 자체는 guard 원인이 아니다. | 잘못된 protected item을 quarantine하고 보호 정책은 약화하지 않는다. |
| 12 | `maintenance_enqueued` | **확정 표기 결함** | 실제 worker queue가 아니라 audit shadow 한 건이다. | `maintenance_audit_recorded`로 바꾸고 queue depth를 0으로 표시한다. |
| 13 | `drift_detected` | **문구 과장** | 현 경로에서는 maintenance shadow handoff일 수 있다. | 실제 drift와 단순 audit handoff를 별도 코드로 표시한다. |
| 14 | Memory Explorer가 128이 아닌 120에서 끝남 | **원인 일부 확정** | backend canonical tail이 120에 머물렀다면 Explorer는 저장소 사실을 읽은 것이다. | host count/backend tail/pending queue를 같은 화면에서 비교한다. |
| 15 | 새 세션에도 `vector_not_configured` | **현재 턴의 서버 공통 경고** | 다른 세션의 과거 오류가 아니라 현재 backend의 Chroma endpoint 미설정이다. | OS별 managed vector 기동 후 endpoint·upsert·readback을 검증한다. |
| 16 | Windows만 Vector가 꺼짐 | **3.5 패키징 결함, 3.6 소스 보완·실환경 미검증** | 관리형 runtime 설치와 Go profile 연결이 어긋났다. | Windows 관리형 신규 설치와 3.5→3.6 update를 실제 PC에서 검증한다. |
| 17 | ChromaDB 설치 실패 | **3.6 소스 보완·실환경 미검증** | `chromadb==1.5.9` 전용 runtime 경로가 현재 존재한다. | 불완전 기존 Python, 신규 설치, 재실행 세 케이스를 검증한다. |
| 18 | MariaDB 권한 오류 | **소스 보완·실환경 미검증** | 127.0.0.1/localhost 계정과 DB 권한 부여가 있다. | 기존 사용자·암호·host 조합에서 실제 CRUD/DDL probe를 실행한다. |
| 19 | updater `status` 없음 | **호환 계약 미검증** | mixed-package 방지는 맞지만 구 updater의 stdout 계약과 새 launcher가 맞지 않을 수 있다. | 3.0 updater fixture로 `status`/`apply-pending` 호환·복구 test를 추가한다. |
| 20 | updater 이름을 바꾸니 기동됨 | **우회이지 해결 아님** | updater 경로를 건너뛰었을 가능성이 크다. | pending state를 검사하고 명시적 recovery를 제공한다. |
| 21 | 세션 migration 후 데이터 불안 | **P0 확정 결함** | 46개 직접 session 테이블 중 일부만 copy/count한다. | source cleanup을 즉시 fail-closed하고 manifest migration을 구현한다. |
| 22 | 캐릭터 index 변경·attach 불안 | **확정 설계 위험** | pin key는 index 기반이고 pin 저장 실패가 무시된다. | stable character ID + host chat ID backend binding을 사용한다. |
| 23 | 서사 가이드 예산 부족 | **3.6 소스 보완·실환경 미검증** | 현재 최종 `output_guidance` lane 연결은 있다. | 실제 final payload에서 warning 문구와 reference가 함께 전달되는지 검증한다. |
| 24 | 월드 규칙 chars가 예산보다 크게 보임 | **확정 진단 결함** | render transparency가 pre-budget `WorldRulesText`를 집계한다. | eligible/selected/delivered/deferred를 분리한다. |
| 25 | reindex 후보 수와 처리 수가 다름 | **확정 telemetry 결함** | total은 3개 계층 합계인데 응답 일부는 memory 수만 `candidates`로 쓴다. | top-level candidates를 totalCandidates로 통일한다. |
| 26 | OOC인데 `본문 응답 기다리는 중` | **현재 소스 구현 확인·live 회귀 필요** | `OOC_INPUT_CANCELLED` notice 경로가 있다. | 실제 판정→취소→HUD notice 순서를 live 검증한다. |
| 27 | 삭제·리롤 알림 | **현재 소스 구현 확인·live 회귀 필요** | delete/reroll notice 문자열과 HUD 경로가 있다. | 실제 RisuAI 삭제·리롤에서 정확히 한 번 표시되는지 검증한다. |
| 28 | 오류창이 바깥 클릭으로 닫힘 | **현재 소스 구현 확인·live 회귀 필요** | 정보 notice와 attention/error dismiss 규칙이 분리돼 있다. | error는 X로만 닫히고 정상 notice만 바깥 클릭으로 닫히는지 검증한다. |
| 29 | prefill이 사용자 입력으로 저장됨 | **재현 필요** | 현재 제공된 로그로는 host payload와 실제 user message를 구분할 수 없다. | phrase hard-code 없이 input hook·beforeRequest·stored chat의 role/index trace를 수집한다. |
| 30 | AI가 이전 질문을 반복함 | **재현 필요** | 기억 누락, budget defer, model 반복 중 무엇인지 로그가 없다. | 해당 턴의 final delivered context와 backend canonical tail을 확보한다. |
| 31 | `risuFetch is deprecated` | **현재 3.6 소스 해결 확인** | bridge는 `risuai.nativeFetch`를 사용한다. | 3.5 배포물과 3.6 로드 artifact를 구분한다. |
| 32 | `content.js message port closed` | **별도 문제 가능성 높음** | 브라우저 확장 message port 로그만으로 Archive Center 오류라 할 수 없다. | Archive Center request ID와 같은 시각의 실패가 있을 때만 연결한다. |
| 33 | `char_-1` | **원인 아님** | 채팅 미진입 메인 화면의 read-only session 값이다. | 활성 채팅 진입 후 SID와 분리 표시한다. |
| 34 | 28080 bind 오류 | **운영 충돌** | 동일 포트를 다른 backend가 이미 사용 중이다. | 실행 중 PID/버전을 보여주고 중복 기동을 막는다. |
| 35 | HUD `/turn-workflow/status` OK 반복 | **3.7 source 수정·live 회귀 필요** | 반복 long-poll과 `unknown` 120ms 재조회는 제거됐다. | `/turn-workflow/events` NDJSON 한 연결에서 revision을 순서대로 받고 terminal/context/UI timeout에만 종료한다. 스트림 미지원은 typed 알림이며 polling으로 복귀하지 않는다. |
| 36 | UI updater 상태 새로고침 실패 가능성 | **현재 3.6 확정 결함** | `refreshArchiveCenterUpdateStatus()`가 범위 밖의 `$`를 호출한다. | 기존 DOM helper로 교체하고 실제 설정 탭 회귀를 추가한다. |

### 3.1 현재 처리 상태

위 표는 `d167f82` 감사 기준선에서 사용자 제보를 분류한 기록이다. “필요한 조치”는
당시의 해결 조건이며 현재 미구현 목록으로 읽지 않는다.

| 범위 | 현재 상태 |
|---|---|
| #1~6 최종 출력·큐·logical replacement·verify 표기 | 3.7-B source·자동 회귀 완료. durable intent·bounded terminal·reconciliation 포함, 실제 RisuAI lifecycle은 3.7-G |
| #7~13 Critic·JSON·protected quarantine·maintenance 표기 | 3.7-C source 구현·자동 회귀 완료, 실제 provider·worker·DB 장애는 3.7-G |
| #14, #21~22 session tail·migration·binding | 3.7-D 50/50 executor·current parity·binding·동시성 fence source 완료, 실제 DB/Vector 이관은 3.7-G |
| #15~20 Vector·MariaDB·updater | 3.7-D source/package fixture 구현, packaged 신규·upgrade·health 검증은 3.7-G |
| #23~28, #35~36 서사 가이드·HUD·대시보드·updater UI | 3.7-F source 구현, 3.7-E typed current/history card와 severity·reason/time 연결 완료, loaded UI·Host 검증은 3.7-G |
| #29~30 prefill 오인·질문 반복 | 미재현. 실제 role/index/hash trace 없이는 자동 교정하지 않음 |
| #31, #33 | 원인 아님 또는 현 source 해결 확인 |
| #32, #34 | Archive Center와 별도일 수 있는 브라우저 확장·포트 운영 문제 |

## 4. 공식 RisuAI 계약 재확인

기존 상세 감사의 고정 기준은 2026-07-29의 RisuAI `main` HEAD
`846c897bf56e3e0121ef12e7ba07f5c3912dfa78`이다. 2026-07-30에는 공식
`main` raw source, API v3 type definition, 공개 플러그인 문서와 릴리스 목록을
다시 확인했다.

공식 API v3 type definition은 다음을 명확히 한다.

- 문서화되지 않은 API는 내부 또는 변경 가능 API이며 플러그인이 의존하면 안 된다.
- 모든 API 호출은 iframe `postMessage`를 거치는 비동기 호출이다.
- `afterRequest` replacer는 **모델 응답을 받은 뒤 그 응답을 변환하는 hook**이다.
  채팅 DB에 최종 결과가 commit된 뒤의 event라고 정의돼 있지 않다.
- 공식 API에는 현재 채팅/캐릭터 index와 `getChatFromIndex`가 있다.
- DOM 변화를 관측할 수 있는 `createMutationObserver`가 있지만, `getRootDocument()`는
  꼭 필요한 경우가 아니면 사용하지 말라는 경고가 있고 finality 확인 용도로는
  사용하지 않는다.
- 표시 직전 텍스트를 통과시키는 공식 `display` script handler가 있다.
- `character.chaId`는 stable character ID로 문서화돼 있다.
- 현재 type definition에는 `addRisuChatListener`가 없다.

근거:

- [RisuAI API v3 type definition — pinned commit](https://github.com/kwaroran/RisuAI/blob/846c897bf56e3e0121ef12e7ba07f5c3912dfa78/src/ts/plugins/apiV3/risuai.d.ts)
- [afterRequest replacer 계약](https://github.com/kwaroran/RisuAI/blob/846c897bf56e3e0121ef12e7ba07f5c3912dfa78/src/ts/plugins/apiV3/risuai.d.ts#L1677-L1710)
- [display script handler 계약](https://github.com/kwaroran/RisuAI/blob/846c897bf56e3e0121ef12e7ba07f5c3912dfa78/src/ts/plugins/apiV3/risuai.d.ts#L1644-L1666)
- [공식 active chat read API](https://github.com/kwaroran/RisuAI/blob/846c897bf56e3e0121ef12e7ba07f5c3912dfa78/src/ts/plugins/apiV3/risuai.d.ts#L1247-L1271)
- [공식 mutation observer](https://github.com/kwaroran/RisuAI/blob/846c897bf56e3e0121ef12e7ba07f5c3912dfa78/src/ts/plugins/apiV3/risuai.d.ts#L1181-L1198)
- [stable character ID 문서](https://github.com/kwaroran/RisuAI/blob/846c897bf56e3e0121ef12e7ba07f5c3912dfa78/src/ts/plugins/apiV3/risuai.d.ts#L172-L178)

post-commit output event를 추가하는
[PR #1442](https://github.com/kwaroran/RisuAI/pull/1442)는 2026-07-30 현재도
**Open**이다. 이 PR이 `addRisuChatListener('output', fn)`을 별도로 제안한다는 사실은
현 `afterRequest`가 post-commit event가 아니라는 판단과도 일치한다.

따라서 3.7은 `addRisuChatListener`를 기능 탐지해 쓰는 경로도 넣지 않는다. 열린
PR의 미확정 callback shape나 제거 계약을 production 호환 경로로 만들면 현재
지원 Host에서 검증되지 않은 병렬 finality owner가 생기기 때문이다. 공식 merge와
공개 API 계약 확정은 이후 별도 버전에서 다룬다.

### 4.1 감사 기준선에서 확인한 위반 지점 — 3.7-B 이전

아래 내용은 감사 기준 커밋 `d167f82`의 과거 구현을 설명한다. 현재 3.7-B source에는
이 finality 고정값과 synthetic 재진입이 없다.

감사 기준선의 [Archive Center.js](../Archive%20Center.js)에는 다음 고정값이 있었다.

- 감시 시작 지연: 700ms
- 조회 주기: 800ms
- 일반 안정 판정: 3회
- 스트리밍 안정 판정: 4회
- 마지막 변화 후 대기: 8초
- 감시 강제 종료: 90초
- 뒤늦은 native recovery grace: 12초

[active-chat watcher](../Archive%20Center.js#L15698)는 위 시간과 message content
안정성을 사용해 최종 출력을 결정하고,
[synthetic afterRequest](../Archive%20Center.js#L15764)에서 다시
`onAfterRequest(candidate, ...)`를 호출한다.

이는 JavaScript가 관측을 넘어 최종성 정책과 저장 재진입을 소유하는 구조다.
`Archive Center.js`가 얇은 RisuAI adapter여야 한다는
[영구 경계](permanent-risu-host-backend-boundary.md)와 맞지 않는다.

### 4.2 올바른 목표 상태

1. native `afterRequest`는 공식 RisuAI가 해당 요청의 모델 응답을 전달한
   lifecycle 신호다. 채팅 DB post-commit 신호는 아니지만, `beforeRequest`에서
   캡처한 요청 correlation·host chat·user anchor와 응답 hash를 묶은
   `source_acceptance_observation.v3`을 만들 수 있다.
2. JavaScript는 이 관측값만 전달한다. canonical source acceptance, logical turn,
   replacement와 persistence 권한은 Go가 소유한다.
3. Go는 correlation, user/assistant hash parity와 lifecycle을 검증한 뒤
   `accepted`, `replaced`, `deleted`, `superseded`로 전이한다.
4. 응답 본문 반환은 complete-turn/Critic을 기다리지 않는다. 같은 턴의 후속
   저장을 비동기로 시작하되, RisuAI에 보이는 응답은 즉시 반환한다.
5. 다음 공식 `input`/`beforeRequest`의 active-chat 관측은 `afterRequest`가
   누락되었거나 기존 pending 자료를 복구하는 backfill 경로다. 정상 응답을
   다음 턴까지 무기한 보류하는 finality 정책으로 사용하지 않는다.
6. root document 관찰, polling, quiet window, 강제 종료 시간과 synthetic
   `afterRequest` 재진입은 사용하지 않는다.
7. post-commit message ID·generation ID·branch ID처럼 hook에서 노출되지 않은
   값은 추측하지 않는다. v3 관측에는 `not_exposed` 또는 `unobserved`로 남긴다.
8. 이후 리롤·삭제·편집은 새 request correlation과 source revision으로 기존
   결과를 supersede 또는 invalidate한다.
9. 공급자 응답 속도는 finality 의미를 바꾸지 않는다.

### 4.3 3.7-A trace 좌표 기준선

같은 이름을 서로 다른 의미로 사용하지 않도록 현재 좌표를 다음과 같이 고정한다.

| 정식 이름 | producer → consumer | scope·durability | 3.7-A 판정 |
|---|---|---|---|
| orchestration request correlation ID | JS prepare 관측 → Go prepare/HUD lineage | 단일 main request, host 관측값; 공식 Risu request ID 아님 | complete에서는 source-to-final lineage가 만들어진 경우에만 보존된다. 조건부 상관은 3.7-B 차단 항목이다. |
| complete-turn idempotency key | JS final content/generation hash → Go complete idempotency ledger | complete 요청 재전송 단위, durable 처리 키 | orchestration correlation ID와 alias하면 안 된다. JS의 `client_meta.request_id`는 현재 이 값을 다시 담는다. |
| HTTP trace request ID | Go `trace.go` helper | 단일 HTTP 요청용 의도, 현재 production 호출 없음 | 연결된 trace로 간주하지 않고 `unwired` 기준선으로 남긴다. |
| chat session ID | RisuAI active chat 관측 → Go routing/prepare/complete/store | 대화 세션 단위, canonical store key | 요청 SID와 backend가 resolve한 canonical SID를 구분해 표시해야 한다. |
| resolved turn index | Go routing/complete canonical tail | 세션 안의 숫자 턴, 저장된 chat tail에 따라 재결정 가능 | durable `logical_turn_id`와 별개다. |
| logical turn ID | Go accepted user anchor hash → source acceptance/derived fence | 논리 사용자 턴 단위, durable | HUD의 숫자 `logical_turn`과 alias하면 안 된다. |
| host generation ID | RisuAI message 관측 → source acceptance | 선택적 host 관측값, nullable | 미노출이면 `unobserved`; 모델명·시간으로 추정하지 않는다. |
| HUD workflow attempt | Go in-memory workflow ledger | `(session, resolved turn)` 진행 표시, 재시작 시 비영속 | JS transport queue `attempts`와 alias하면 안 된다. |
| transport queue attempts | JS durable failed queue | backend 전송 재시도 횟수 | pending final confirmation에는 사용하지 않는 것이 3.7-C 목표다. |
| source revision | Go accepted session/turn/generation/message/content hash → store/derived fence | canonical source revision, durable | HUD long-poll `revision`과 alias하면 안 된다. |
| HUD ViewModel revision | Go in-memory workflow status revision | long-poll 변경 감지용, 비영속 | source revision이 아니다. |
| migration ID | MariaDB migration row → reindex/lock/rollback/cleanup | whole-session migration 단위, durable | turn/request/source revision을 억지로 결합하지 않는다. |

공식 RisuAI request ID는 현재 노출되지 않으므로
`official_risu_request_id_not_exposed` 상태를 유지한다. 3.7-A에서는 관측되지 않은
공식 ID를 자체 orchestration ID로 대체해 표기하지 않는다.

## 5. 최종 출력·실패 큐 상세

### 5.1 감사 기준선의 최대 시도 횟수 우회 경로 — 3.7-B 이전

아래는 `d167f82`에서 재현한 과거 경로다. 현재 3.7-B source는 pending
confirmation을 실패 큐에서 분리하고 모든 transport 재시도 경로에 유한 상한을
적용한다.

감사 기준선의 `drainFailedQueue`는 item의 `attempts`를 증가시킨 뒤
다음 경우 곧바로 `stillFailed`에 넣고 `continue`한다.

- queued complete-turn의 source observation을 갱신하지 못함
- request status가 `processing`
- request status가 `completed`이지만 `success != true`

최대 시도 횟수 검사는 이보다 아래에 있으므로 위 경로에는 도달하지 않는다.
따라서 `att=36`, `att=223`과 같은 장기 잔존이 가능하다.

### 5.2 실패 큐의 목표 분리

| 상태 | attempts 증가 | 자동 재시도 | HUD |
|---|---:|---:|---|
| `pending_confirmation` | 아니오 | host signal 때만 재평가 | 정보 |
| `backend_processing` | 아니오 | status event/long poll | 진행 |
| `retryable_transport_failure` | 예 | 사용자 retry 설정 이내 | 경고 |
| `retryable_critic_failure` | backend job 소유 | worker policy 이내 | 경고 |
| `terminal_source_rejection` | 아니오 | 없음 | 정보 또는 경고 |
| `terminal_contract_failure` | 아니오 | 없음 | 오류 |
| `completed` | 아니오 | 없음 | 성공 |
| `superseded_or_deleted` | 아니오 | 없음 | 알림 |

JavaScript 로컬 queue에는 backend-unreachable transport item만 남겨야 한다.
canonical turn, source acceptance, critic retry는 Go의 durable state로 이동해야 한다.

### 5.3 DeepSeek와 GLM 차이

DeepSeek의 긴 reasoning/streaming 또는 결과의 후반 변화는 8초 quiet와 stable poll을
자주 리셋할 수 있다. GLM의 결과가 더 빨리 안정되면 같은 잘못된 경로가 통과할 수
있다. 이 차이는 재현 조건이지 root cause가 아니다.

따라서 다음 구현은 금지한다.

- `if model contains deepseek`
- DeepSeek 전용 180초 timeout
- provider별 안정 poll 횟수
- 특정 output 문구를 최종성 신호로 사용

## 6. `LOGICAL_TURN_REPLACE_FAILED`

실패 위치는
[group_turn_complete.go](../go-service/internal/httpapi/group_turn_complete.go#L147)의
logical turn replacement다. 현재 HUD에는 모든 하위 오류가
`LOGICAL_TURN_REPLACE_FAILED`로 합쳐지고 HTTP 500 body에만 원래 오류가 붙는다.

[mariadb_logical_turn_replace.go](../go-service/internal/store/mariadb_logical_turn_replace.go#L12)는
다음만 허용한다.

- 요청 turn이 현재 canonical tail
- RisuAI가 기존 assistant를 지운 직후라 tail이 정확히 하나 비어 있는 경우

과거 turn 또는 gap은 거부한다. 그 외에도 다음 하위 오류가 가능하다.

- source revision mismatch
- MariaDB 권한 부족
- FK 또는 transaction 실패
- session routing mismatch
- 실제 canonical tail과 요청 turn 불일치

사용자 screenshot만으로 이 중 하나를 확정할 수 없다. 반드시 500 응답 body와
`request_id`, `sid`, `requested_turn`, `latest_tail`, source revision을 함께 받아야 한다.

목표 stable error:

- `LOGICAL_TURN_NOT_CURRENT_TAIL`
- `LOGICAL_TURN_REVISION_CONFLICT`
- `LOGICAL_TURN_SESSION_MISMATCH`
- `LOGICAL_TURN_DB_PERMISSION_DENIED`
- `LOGICAL_TURN_FK_CONFLICT`
- `LOGICAL_TURN_TRANSACTION_FAILED`

tail/revision/session mismatch는 반복 재시도로 해결되지 않으므로 terminal이다.
DB transport/temporary transaction만 제한적으로 retryable이어야 한다.

## 7. 저장 성공과 read-back 실패를 구분

다음 응답은 저장 실패가 아니다.

```text
raw:ok
derived:ok
complete-turn verify_unavailable
raw_chat_log_verify_unavailable
```

[Archive Center.js](../Archive%20Center.js#L6199)는 canonical read-back timeout을
사용자 설정과 무관하게 최대 2500ms로 자른다. 원격 Tailscale, 모바일, 느린 DB에서는
쓰기 성공 후 이 선택적 검증만 실패할 수 있다.

목표 표기:

- `raw:ok` → Raw Save 성공
- `derived:ok` → Derived Save 성공
- `verify_unavailable` → `정보: 저장 응답은 성공, 선택적 read-back 미확인`
- read-back에서 실제 불일치 → `경고: write/read mismatch`

complete-turn 응답을 write authority로 유지하고 read-back 실패 때문에 이미 성공한
턴을 실패 큐에 다시 넣지 않는다.

## 8. 평론가 LLM과 JSON

### 8.1 현재 3.7에서 적용된 구조화 출력

[turn_extraction_critic.go](../go-service/internal/httpapi/turn_extraction_critic.go#L701)는
현재 raw JSON candidate를 먼저 파싱하고, 실패한 경우에만 제한된 repair를 적용한다.
과거처럼 먼저 전체 텍스트의 따옴표를 무조건 바꾸는 경로는 아니다.

[proxy_provider.go](../go-service/internal/httpapi/proxy_provider.go#L329)는
critic/extraction JSON 요청에만 구조화 출력을 요구한다.

- Gemini/Vertex:
  `generationConfig.responseMimeType=application/json`
- Claude:
  `output_config.format.type=json_schema`와 Critic 최상위 필드 schema
- 공식 OpenAI 호환 구조화 출력을 확인한 OpenAI/OpenRouter/LLM Gateway:
  `response_format.type=json_object`
- Vercel AI Gateway:
  공식 Chat Completions 계약의 `response_format.type=json_schema`와 object schema

사용자가 제공한 같은 목적의 `json_schema`는 보존하고 충돌하는 출력 형식은 upstream
호출 전에 typed request-build 오류로 중단한다. 일반 서사 호출에는 이 정책을
강제하지 않는 회귀 테스트가 있다. Custom/Ollama/Copilot은 공통 구조화 출력 계약을
추정하지 않는다. 사용자가 Extra Body JSON으로 명시한 공급자 형식은 보존·검증하지만
확인되지 않은 `response_format`을 backend가 임의로 추가하지 않는다.

이 변경은 이전 3.5의 `invalid character ... after array element`,
`critic_json_missing` 문제에 직접 대응한다.

### 8.2 3.7-C에서 처리한 오류 분류

과거 [group_turn_complete.go](../go-service/internal/httpapi/group_turn_complete.go#L537)는
다음을 모두 `CRITIC_LLM_FAILED`로 표시했다.

- upstream HTTP/auth/quota 오류
- timeout
- content 추출 결과 없음
- JSON object 없음
- brace/bracket mismatch
- JSON parse 실패
- normalized schema 불일치

현재는 provider 호출, timeout, 빈 응답, JSON 누락, JSON 문법, schema 오류를
각각 분리하고 길이가 제한되며 credential이 제거된 `raw_preview`를 보존한다.
이 trace를 사용하지 않고 모델이 잘못했다고 결론 내리면 안 된다.

목표 error 분류:

| 단계 | stable code | retryable |
|---|---|---:|
| 공급자 HTTP/auth/quota | `CRITIC_UPSTREAM_FAILED` | 상태별 |
| 요청 timeout | `CRITIC_TIMEOUT` | 예 |
| content 없음 | `CRITIC_EMPTY_CONTENT` | 제한적으로 예 |
| JSON object 없음 | `CRITIC_JSON_MISSING` | 예 |
| JSON syntax | `CRITIC_JSON_PARSE_FAILED` | 예 |
| schema 검증 | `CRITIC_SCHEMA_INVALID` | 입력에 따라 |
| stale revision | `CRITIC_RESULT_SUPERSEDED` | 아니오 |

### 8.3 OpenAI 형식에서는 되고 Vertex 형식에서는 안 된 경우

이는 같은 모델이라도 다음 adapter 차이로 생길 수 있다.

- response candidate/content 추출 위치
- tool/structured response shape
- streaming chunk 합성
- JSON MIME 및 기존 `extra_body_json` 충돌
- finish reason과 빈 content 처리

필수 fixture는 “정상 JSON”, “markdown fence JSON”, “빈 content”, “truncated JSON”,
“provider error body”, “streamed candidate”의 공급자별 raw response다.

### 8.4 재처리 안전성

raw user/assistant chat을 먼저 canonical하게 저장한다. critic 실패는 raw를 되돌리지
않고 derived retry job을 만든다. retry 결과가 도착했을 때 source revision이 바뀌었으면
저장하지 않고 superseded 처리한다.

### 8.5 Flex와 캐싱 provider 계약

- OpenAI, LLM Gateway, Vercel AI Gateway, Custom은 설정 UI의
  `OpenAI-Compatible Service Tier`를 공유한다. 내부 저장 필드 이름은 기존 설정과의
  호환을 위해 `llm_gateway_service_tier`를 유지한다.
- `standard`는 upstream 요청에서 생략하고, 사용자가 Flex 또는 Priority를 명시했을
  때만 `service_tier`를 보낸다. 적용 가능 provider와 값 정규화·충돌 판정은 Go가
  소유한다. JavaScript의 provider 목록은 설정 행을 보이거나 숨기는 UI 표현에만
  사용한다.
- 지원하지 않는 provider/model이 반환한 오류를 보고 없이 Standard로 자동 강등하지
  않는다.
- Extra Headers/Body JSON은 Vertex 전용이 아니라 모든 generation provider에서 사용할
  수 있다. `model`, `messages`, 인증 header 같은 정본 필드는 덮어쓰지 못한다.
- OpenAI, Gemini, Vertex, LLM Gateway의 암시적 캐싱은 공급자 계약에 따라 자동이다.
  Vercel의 provider-aware 자동 캐싱은 Extra Body JSON의
  `providerOptions.gateway.caching=auto`로 선택할 수 있다. Custom은 공통 캐시
  표준이 없으므로 공급자 문서의 header/body만 전달한다.
- Gemini/Vertex의 `usageMetadata.cachedContentTokenCount`와 `trafficType`을 응답과
  trace에 보존한다. 반환되지 않은 캐시 적중이나 traffic type은 추정하지 않는다.

세부 계약과 공식 문서 링크는
[`provider-request-overrides-flex-paygo-contract.md`](provider-request-overrides-flex-paygo-contract.md)에
기록한다.

## 9. 보호 기억과 기억 오염

[prepare_turn_memory.go](../go-service/internal/httpapi/prepare_turn_memory.go#L205)는
`protected_secrets`와 `character_identity_accuracy`만 보호 guard 입력으로 읽는다.
[turn_extraction_critic.go](../go-service/internal/httpapi/turn_extraction_critic.go#L1081)의
`belief_updates`는 별도 필드다. 따라서 `belief_updates`가 직접 guard를 만들었다는
인과관계는 현재 소스에서 확인되지 않는다.

다만
[protectedSecretRequiresGuard](../go-service/internal/httpapi/prepare_turn_memory.go#L766)는
policy가 비어 있으면 fail-closed로 guard를 활성화한다. 안전 원칙 자체는 맞지만,
LLM이 owner/policy/evidence가 없는 protected item을 많이 만들면 guard가 과도해질 수
있다.

해결 방향은 보호를 끄는 것이 아니다.

1. owner, subject, policy, evidence가 부족한 protected item은
   `protected_policy_missing` quarantine으로 보낸다.
2. quarantine item은 public memory에도 private guard에도 주입하지 않는다.
3. 같은 secret identity + owner의 guard는 한 번만 렌더링한다.
4. guard lane에 독립 예산과 최대 item 수를 둔다.
5. trace에 source field, policy, defaulted 여부, evidence를 남긴다.

필수 인과 회귀:

- `belief_updates`만 존재 → guard 0
- 같은 fixture에 valid `protected_secrets` 추가 → guard 1
- policy/owner/evidence 누락 protected item → quarantine 1, guard 0

### 9.1 이미 오염된 파생 기억 복구

1. raw chat과 effective input을 authority로 고정
2. 해당 source revision에서 생성된 derived row를 stale 처리
3. 현재 parser/schema로 재추출
4. malformed protected item quarantine
5. memory/evidence/KG/rule/status를 새 revision으로 rebuild
6. vector outbox 재생
7. 이전 derived/vector를 supersede

원본 raw chat을 삭제하거나 사용자 DB를 새 패키지에 포함해서 해결하면 안 된다.

## 10. maintenance와 audit 로그 해석

[group_turn_complete.go](../go-service/internal/httpapi/group_turn_complete.go#L1606)는
명시적으로 `worker_enabled=false`, `queue_mode=audit_shadow`라고 기록하면서도
`QueueDepth=1`, `Enqueued=true`, `maintenance_enqueued` audit event를 만든다.
실제로 실행 가능한 worker queue에 넣은 것이 아니다.

목표 응답:

```json
{
  "maintenance_enqueued": false,
  "maintenance_audit_recorded": true,
  "maintenance_queue_depth": 0,
  "maintenance_queue_mode": "audit_only"
}
```

사용자에게 제공된 로그는 다음처럼 읽어야 한다.

| 로그 | 의미 |
|---|---|
| `source_acceptance active final turn N` | turn N 출력이 active final source로 받아들여짐 |
| `source_acceptance replacement complete turn N` | 같은 logical turn의 새 출력으로 교체 완료 |
| `effective_input_saved` | 실제 입력 조립 결과 저장 |
| `critic_ingest_trace` | critic 결과 처리 trace 저장 |
| `critic_extract_failed` | raw는 보존됐지만 derived 추출 실패 |
| `maintenance_enqueued` | 현 소스에서는 실제 worker queue가 아니라 audit shadow |
| `drift_detected ... shadow handoff` | 실제 수리 완료가 아니라 후속 점검 audit |
| `repair_replay restored 2 chat log roles` | user/assistant raw 역할 2개를 canonical log에 복구 |
| `session_normalize` | 세션 정규화 관리 작업 |
| `admin_reindex` | vector 재색인 관리 작업 요청 |

같은 turn의 `critic_extract_failed`가 여러 번 보이면 첫 실행, queue retry, repair/reindex
재처리를 구분할 `attempt_id`와 `source_revision`이 필요하다.

## 11. 입력 예산·서사 가이드·투명성

현재 실제 long-term memory 전달 owner는
[buildPrepareTurnMemoryDeliveryPlan](../go-service/internal/httpapi/prepare_turn_memory_budget.go#L117)이며,
최종 텍스트는 `final_text`다.
[prepare_turn_assembly.go](../go-service/internal/httpapi/prepare_turn_assembly.go#L579)가
plan을 만들고 Go payload application plan이 최종 auxiliary text를 구성한다.

반면 [prepare_turn_render.go](../go-service/internal/httpapi/prepare_turn_render.go#L394)는
pre-budget `assembly.WorldRulesText`를 transparency block에 그대로 넣는다. 따라서
`world_rules 12272 chars / budget 2500`처럼 보여도 그것이 최종 주입 12272 chars라는
뜻은 아닐 수 있다. UI가 이 차이를 숨기는 것이 결함이다.

각 lane은 다음을 모두 보여야 한다.

- `eligible_count`, `eligible_chars`
- `selected_count`, `selected_chars`
- `delivered_count`, `delivered_chars`
- `deferred_count`, `deferred_chars`
- `deduplicated_count`
- `reason_code`

`count > 0`, `delivered_chars == 0`이면 단순 `skipped`가 아니라
`deferred_insufficient_narrative_budget`, `deduplicated`,
`blocked_by_privacy`, `not_relevant` 중 실제 이유를 표시한다.

### 11.1 서사 가이드

현재 소스는
[fidelity_warnings](../go-service/internal/httpapi/prepare_turn_render.go#L182)을
`output_guidance` lane으로 만들고,
[group_turn_prepare.go](../go-service/internal/httpapi/group_turn_prepare.go#L816)가
최종 auxiliary text에 연결한다. `0aeead7`의 source-backed narrative guidance
수정도 존재한다.

그러나 실제 RisuAI provider payload에서 모든 전달 warning 문구가 해당 reference와
함께 들어갔다는 live 증거는 없다. 상태는 `implemented_unverified`다.

배포 전 반드시 확인한다.

- Weak/Medium/Strong 각각 최소 1회
- auto budget 3000에서 작은 guidance가 전달되는지
- 큰 guidance가 defer돼도 뒤의 작은 guidance는 전달되는지
- HUD의 delivered chars가 실제 provider payload와 같은지
- protected context가 fidelity warning으로 노출되지 않는지

## 12. 세션 migration P0

병렬 감사와 현재 소스를 다시 대조한 결과,
`migrations/001_schema.sql`에는 migration metadata를 제외하고 직접 session ID를 가진
업무 테이블이 46개다.

[mariadb_migration.go](../go-service/internal/store/mariadb_migration.go#L146)의 copy/count는
다음 root만 다룬다.

- `chat_logs`
- `effective_input_logs`
- `memories`
- `direct_evidence_records`
- `kg_triples`
- `episode_summaries`
- `protagonist_entity_memories`
- `session_reference_bindings`
- reference runtime child

### 12.1 source cleanup에서 삭제되지만 복사되지 않는 20개

```text
critic_feedback
persona_capsule_attachments
character_events
entities
trust_states
storylines
guidance_plan_states
world_rules
session_active_scopes
character_states
pending_threads
active_states
canonical_state_layers
chapter_summaries
arc_summaries
saga_digests
consequence_records
psychology_branches
session_fork_lineage
theme_offscreen_carries
```

### 12.2 복사되지 않고 source cleanup 뒤에도 원본 SID에 남는 18개

```text
audit_logs
persona_memory_capsules
capture_verification_records
status 계열 5개
entity identity 계열 5개
precise_memory_units
derivation 계열 4개
```

추가로:

- `persona_memory_entries`는 capsule 아래 간접 child로 원본에 남는다.
- reference coverage snapshots/fields는 target에 복사되지 않고 source binding cascade로
  삭제된다.
- source direct evidence 삭제는 남은 precise-memory
  `root_evidence_id`를 `ON DELETE SET NULL`로 바꿀 수 있다.
- 그럼에도 cleanup 결과는
  [ReadyForLive=true](../go-service/internal/store/mariadb_migration.go#L634)를 반환할 수 있다.

이는 단순 telemetry가 아니라 실제 데이터 손실·분산 위험이다.

### 12.3 원자성 위험

- copy는 `READ COMMITTED` 여러 SELECT이며 copy 전 source write fence가 없다.
- preview hash를 complete에서 강제하지 않는다.
- source lock은 vector 단계 이후다.
- Chroma source를 먼저 지우고 MariaDB cleanup이 실패하면 두 저장소가 갈라진다.
- vector 후보는 memory/episode 중심이며 expected ID parity를 증명하지 않는다.
- omitted table에만 target row가 있어도 “target empty”로 판정될 수 있다.

### 12.4 즉시 조치

**전체 manifest migration이 완성되기 전까지
`/sessions/migrate-cleanup-source`의 실제 cleanup을 fail-closed한다.**
dry-run은 허용해도 confirm cleanup은 차단한다.

#### 2026-07-29 3.7-A 적용 기록

- cleanup 응답 계약을 `sc-mig-cleanup.v2`로 올렸다.
- dry-run은 source/target ID, migration ID, row/vector count를 읽기 전용으로
  반환하되 `session_cleanup_manifest_verification_required` blocker와
  `ready_for_cleanup=false`를 반드시 반환한다.
- confirm 요청도 같은 blocker로 끝나며 MariaDB cleanup과 Chroma
  `DeleteSession`을 호출하지 않는다.
- HTTP handler를 우회해 store cleanup을 직접 호출해도
  `ErrSessionMigrationCleanupManifestUnverified`로 실패한다.
- 기존 부분 삭제 helper는 제거했다. 전체 manifest가 완성되기 전까지 별도
  compatibility cleanup 경로를 두지 않는다.

자동 회귀에서는 dry-run/confirm/store 직접 호출의 무삭제와 기존 Go package 회귀를
확인했다. 이는 실제 MariaDB/ChromaDB가 연결된 live migration 증거는 아니다.
46개 전체 manifest, count/hash/FK/vector parity와 durable saga는 후속 migration
packet의 배포 차단 조건으로 남는다.

전체 구현은 모든 session table을 다음 중 정확히 하나로 분류해야 한다.

- `copy`
- `retain_audit`
- `regenerate`
- `delete_after_verified`

source count/hash, target count/hash, row map, FK remap, vector expected ID parity가 모두
맞아야 `copy_verified → vector_verified → source_locked`로 진행한다.
MariaDB와 Chroma 외부 작업은 durable saga로 복구 가능해야 한다.

## 13. 세션 ID, 캐릭터 이동, pin

현재 좋은 기반:

- RisuAI `chat.id`를 opaque host ID로 관측한다.
- Go는 `_cid_<hostChatID>` suffix로 기존 session을 찾을 수 있다.
- RisuAI 공식 API는 stable `character.chaId`를 문서화한다.

현재 위험:

- [makeSessionPinKey](../Archive%20Center.js#L5521)는 `charIdx + chatIdx`다.
- [savePinnedSessionId](../Archive%20Center.js#L8190)는 저장 실패를 log만 하고 상위 호출에
  실패를 반환하지 않는다.
- migration target은 현재 index와 chat ID로 새 SID를 조립한다.
- backend `ListSessions`는 `chat_logs`, `memories`, `kg_triples`만 UNION하므로 다른
  테이블에만 존재하는 session은 후보에서 사라질 수 있다.

목표:

```text
stable character ID + opaque host chat ID
    -> backend durable route binding
    -> canonical Archive Center session ID
```

index는 lookup coordinate일 뿐 identity가 아니다. manual attach와 migration 성공은
backend binding 저장과 read-back acknowledgement가 성공한 뒤에만 UI에 성공으로
표시한다. locked source SID가 들어오면 lock ledger의 target으로 redirect한다.

## 14. VectorDB와 OS 패키지

`vector_not_configured`는 세션별 과거 경고가 아니다.
[turn_extraction_vector.go](../go-service/internal/httpapi/turn_extraction_vector.go#L30)는
현재 서버에 Chroma endpoint가 없으면 현재 turn에 이 상태를 반환한다. 서버 공통
설정이므로 새 세션에서도 반복된다.

현재 Windows 소스:

- managed 패키지 기본 profile: `full_local`
- managed vector mode: local bundled/managed Chroma
- 별도 Python runtime에 `chromadb==1.5.9`
- API path `/api/v2`
- runtime 누락 또는 버전 불일치 시 fail-closed
- commit `d167f82`에서 Windows 3.6 managed vector runtime 연결 보완

따라서 판정은 다음처럼 나뉜다.

- **3.5 Windows 배포물에서 endpoint가 비어 있었다면 실제 패키징 결함**
- **현재 3.6 source는 보완됨**
- **실제 3.5→3.6 updater가 runtime 설치·env 연결·기존 data 보존까지 수행하는지는
  아직 미검증**

OS 간 계약은 같아야 한다.

1. 사용자 실행 시 필요한 MariaDB/Chroma를 설치
2. 사용자 `.env`, API key, MariaDB data, Chroma persist data는 패키지에 포함하지 않음
3. 기존 data directory 보존
4. ready에서 MariaDB와 Chroma를 각각 검증
5. 한 문서 upsert 후 exact read-back 검증

## 15. MariaDB 권한

현재 managed 설치 소스에는 다음이 있다.

- `archive_center@127.0.0.1` create/alter/grant
- `archive_center@localhost` create/alter/grant
- `archive_center.*`에 대한 권한 부여

근거:

- `go-service/cmd/mariadb-schema/main.go`의 managed privilege sync
- `go-service/cmd/managed-mariadb-e2e/managed_command.go`의 managed user setup

하지만 연결 성공만으로 실제 upgrade 권한을 증명할 수 없다.
기존 설치에서 다음 live probe가 필요하다.

- SELECT
- INSERT/UPDATE/DELETE
- additive CREATE/ALTER INDEX 또는 필요한 DDL
- transaction rollback
- 127.0.0.1과 localhost 양쪽 DSN

권한 실패를 `LOGICAL_TURN_REPLACE_FAILED`나 `complete-turn failed`에 묻지 말고
`DB_PERMISSION_DENIED`와 실패 SQL operation class로 표시한다. 비밀번호나 전체 DSN은
로그에 노출하지 않는다.

## 16. updater

Windows launcher는 `status`와 `apply-pending` 응답에 status가 없으면 mixed package를
막기 위해 기동을 중단한다. fail-closed 원칙은 맞다. 문제는 3.0 updater가 새 launcher가
기대하는 JSON contract를 항상 반환한다는 호환 증거가 없다는 점이다.

파일 이름을 바꿔 updater를 건너뛰면 실행될 수 있지만 다음을 증명하지 못한다.

- pending update가 없었는지
- 일부 파일만 교체된 mixed package가 아닌지
- DB/schema update가 완료됐는지
- 이전 backend로 안전하게 rollback 가능한지

필수 updater fixture:

- 3.0.0 clean package
- 3.0.1 package
- 3.5.0 package
- status가 없는 legacy stdout
- pending state 없음
- apply 도중 중단
- backend health 실패
- 신규 Chroma runtime 필요
- 기존 MariaDB schema additive upgrade

업데이트는 코드 전체 교체와 additive DB schema, managed dependency 설치를 지원해야
하지만 사용자 `.env`, DB data, Chroma data를 덮어쓰면 안 된다.

### 16.1 현재 확인된 UI 결함

[refreshArchiveCenterUpdateStatus](../Archive%20Center.js#L13261)는
`$("mo-update-status")`를 호출하지만 해당 `$`는 이 함수 범위에서 정의되지 않는다.
이는 complete-turn 문제와 별개인 설정 탭 updater UI 결함이다.

## 17. HUD와 대시보드

현재 소스에는 다음 notice가 있다.

- OOC 인식 및 입력 처리 취소
- 삭제 확인
- 리롤 확인
- 중복 기존값 보존
- 중복 충돌 보존

현재 구현 여부와 실제 RisuAI lifecycle에서 정확히 표시되는지는 별개다.

목표 severity:

| 수준 | 예 | 닫기 |
|---|---|---|
| 성공 | raw/derived/vector 완료 | 바깥 클릭 또는 자동 |
| 정보 | OOC 취소, 삭제 확인, 리롤 확인, verify unavailable | 바깥 클릭 가능 |
| 경고 | critic retry queued, vector retrying, duplicate conflict | X 권장 |
| 오류 | canonical save 실패, logical replacement DB 실패 | X만 허용 |

HUD는 각 단계의 실제 owner와 상태를 보여줘야 한다.

- 현재 turn / logical turn
- 입력 관측
- prepare-turn
- 기억 retrieval
- 최종 payload 조립
- 본문 provider 응답 대기
- active output 관측
- canonical raw save
- critic LLM
- derived admission
- vector outbox/upsert
- optional read-back

`6/12 본문 응답 기다리는 중`에서 OOC가 이미 확정되면 workflow를
`OOC_INPUT_CANCELLED` terminal notice로 바꾸고 대기 상태를 남기지 않는다.

## 18. Memory Explorer와 턴 수

RisuAI 화면이 128 turn인데 Explorer dry-run이 120에서 끝났다면 다음 세 값을 비교해야
한다.

1. active RisuAI chat의 실제 user/assistant pair 수
2. backend canonical `MAX(turn_index)`
3. `pending_confirmation` 또는 실패 큐의 turn 목록

backend tail이 120이고 121–128이 waiting/queue에 있다면 Explorer가 120까지 검사한
것은 Explorer 버그가 아니라 저장 pipeline 지연의 결과다. 반대로 backend tail이
128인데 Explorer가 120이면 Explorer range 계산 결함이다.

현 HUD/대시보드는 이 비교를 한 화면에 제공하지 않아 사용자가 “검사 완료”를 잘못
해석할 수 있다.

## 19. 재현되지 않은 두 제보

### 19.1 user-ending prefill 오인

현재 제공된 자료에서는 재현되지 않았다. 특정 prompt 문구, `User Input` 문자열,
prefill 끝 문장을 hard-code해 제외하면 안 된다.

필요 trace:

- input hook에서 관측한 원문
- beforeRequest message role/index/content hash
- active chat의 실제 user message role/index/content hash
- effective input의 source role/index
- assistant prefill field가 공식 API에 노출되는지 여부

공식 API가 구분 정보를 제공하지 않으면 `unobserved`로 남기고 자동 교정하지 않는다.

### 19.2 AI 질문 반복

정확한 로그가 없어 원인 미정이다. 반복은 다음 중 하나일 수 있다.

- 이전 답변이 raw에 저장되지 않음
- derived memory 생성 실패
- memory가 예산 또는 privacy로 defer됨
- provider payload에 전달됐지만 모델이 반복함

해당 turn의 `final delivered context` 없이는 Archive Center 결함으로 확정하지 않는다.

## 20. 3.7 해결 순서

### 3.7-A — 기준선과 위험 차단

1. 현재 request/session/turn/generation/revision/migration 좌표의 producer,
   consumer, durability와 nullable 여부를 고정한다.
2. 같은 `request_id`, `revision`, `attempt` 표기가 서로 다른 의미를 공유하는
   지점을 별도 이름과 scope로 기록한다.
3. 전체 manifest parity 없이 가능한 session source cleanup을 HTTP와 store 양쪽에서
   fail-closed한다.
4. 이후 packet이 증명해야 할 재현 fixture와 release-blocking gate를 고정한다.

3.7-A는 finality·queue·critic·전체 migration을 한 번에 고치는 단계가 아니다.
현재 확인된 비가역 손실 경로를 먼저 닫고, 다음 수정이 비교할 수 있는 기준선을
만드는 preparatory safety slice다.

### 3.7-B — 출력 확정과 큐 정상화

1. finality watcher의 고정 시간 정책과 synthetic `onAfterRequest` 재진입 제거
2. pending confirmation을 실패 큐에서 분리
3. 모든 queue 상태에 terminal/retryable 계약과 최대 횟수 적용
4. logical replacement 하위 오류 typed 분류
5. 저장 성공과 optional verification 실패 분리

#### 처리한 사용자 피드백

3.7-B는 다음 보고를 직접 대상으로 삼았다.

- DeepSeek에서는 `waiting for RisuAI active chat confirmation`과
  `bridgeFetch OK /turn-workflow/status`가 반복되고 GLM에서는 정상 완료되던 현상
- 감시 시작 700ms, 조회 800ms, 출력 변화 후 8초, 강제 종료 90초,
  late-native 12초처럼 UI 설정과 무관한 고정 시간이 finality를 결정하던 문제
- `synthetic_afterRequest`가 native hook과 같은 저장 경로에 다시 들어가 중복 후보와
  대기를 만들던 문제
- pending confirmation이 `complete_turn` 실패 큐로 섞여 시도 횟수가
  30회 이상 증가하고 cold start 뒤에도 `deferred`로 남던 문제
- 리롤에서 `LOGICAL_TURN_REPLACE_FAILED` 하나만 보여 권한·revision·deadlock·commit
  결과 불명을 구분하지 못하고 같은 raw를 다시 보낼 위험
- `raw:ok`, `derived:ok`인데 `verify_unavailable` 때문에 전체 저장 실패처럼 보이던 문제

이 단계는 평론가 JSON 내용 자체를 고치는 작업이 아니다. host의 최종 출력 확정,
raw complete-turn exactly-once, pending/failed queue와 저장 결과의 사실성을 교정한
것이다. Critic/provider·파생 기억 문제는 3.7-C가 소유한다.

#### 구현된 계약

- `beforeRequest`에서 현재 character index, chat index, host chat ID,
  message count와 orchestration request correlation을 관측한다.
- native `afterRequest`는 해당 correlation의 공식 모델 응답을
  `source_acceptance_observation.v3`으로 전달한다. 이는 채팅 DB commit을
  주장하지 않으며, 노출되지 않은 active-chat message·generation·branch 사실을
  만들지 않는다. Go가 correlation과 user/assistant hash parity를 검증한 뒤
  같은 턴 accepted final로 전이한다.
- accepted final의 complete-turn/Critic은 비동기로 시작하므로 `afterRequest`
  응답 반환과 화면 출력을 막지 않는다.
- 공식 RisuAI `display` handler는 렌더링 경로이며 finality wake나 commit 증거로
  사용하지 않는다. 다음 공식 `input`/`beforeRequest` active-chat 확인은
  `afterRequest` 누락이나 기존 pending 복구에만 사용한다. 새 출력과 같은 길이
  reroll은 새 request correlation/source revision으로 구분한다.
- finality 경로의 `getRootDocument()`·`createMutationObserver()`와 root subtree
  관찰은 전부 제거했다. 특히 `characterData` mutation의 `Text` target을 RisuAI
  `SafeElement`가 감싸면서 `getAttribute`를 호출하던 live-host 예외 경로를
  제거했다.
- 700ms 시작 지연, 800ms polling, 3/4회 안정 횟수, 8초 quiet window,
  90초 강제 종료, 12초 late-native grace와 synthetic `onAfterRequest` 재진입은
  finality 경로에서 제거했다. promoted assistant sync에 남아 있던 별도 8초
  rollback grace도 제거했으며, exact active-chat confirmation 자체를 근거로
  확정 상태를 기록한다.
- host 확인 대기는 실패 큐와 분리된 `pending final confirmation` lane에 둔다.
  실행 closure는 메모리에만 두되 credential-free complete-turn payload,
  required observation key와 idempotency key는 별도 pluginStorage에 먼저
  영속한다. transport/backend 실패 queue에 넣거나 retry attempt를 올리지
  않는다.
- pending 항목은 session이 아니라 host request 또는 complete-turn
  idempotency key로 식별한다. 같은 session의 겹친 request를 하나의 mutable
  전역값으로 합치지 않으며, 새 후보가 이전 request를 대체하면 이전 후보를
  `superseded`로 끝낸다.
- host read는 시간 polling으로 기다리지 않는다. 다음 공식 host signal마다 정확히
  한 번 관측하고, 동일 observation은 새 source revision이 확인되기 전에는 다시
  처리하지 않는다. accepted context의 duplicate hook은 저장을 다시 시작하지 않는다.
- exact host final이 확인된 continuation은 request-local orchestration,
  effective-input, pending context snapshot과 in-flight fence로 한 번만 실행한다.
- plugin reload 때 별도 pending 저장소를 복원하고 동일 idempotency key가
  실패 큐에도 남아 있으면 pending lane을 우선하여 정확히 한 곳에 둔다.
  복원 payload는 RisuAI active final을 다시 관측한 뒤에만 재개하며, 저장소에는
  critic/embedding 비밀값을 넣지 않는다. 실제 POST 직전에는 현재 UI runtime
  provider 설정을 다시 붙이고 다음 영속화에서는 다시 제거한다.
- `persistentSet`은 pluginStorage 쓰기 실패를 성공으로 삼키지 않는다.
  failed queue 저장 성공이 확인되기 전에는 pending 원본을 삭제하지 않으며,
  pending 저장소는 UI에 보이는 queue capacity를 넘는 새 항목을 조용히
  축출하지 않고 admission 실패로 반환한다.
- failed queue는 실제 transport/backend retryable 실패만 센다. 최대 횟수는
  숨은 상수나 LLM 재시도 값이 아니라 UI에 노출된 `실패 큐 최대 시도 횟수`
  설정을 사용한다. `retryable`, `terminal`, `superseded`를 분리하고
  `terminal`·`superseded`는 재큐잉하지 않는다. backend의
  `status=error`도 `retryable=false` 또는 `queue_action=discard`이면 terminal로
  처리한다.
- complete-turn idempotency ledger는 server-side payload fingerprint를 함께
  보관한다. fingerprint는 raw/source뿐 아니라 context, improvement trace,
  output language, request type, client metadata까지 포함한 complete-turn
  semantic payload 전체를 대상으로 한다. 같은 key와 다른 payload 충돌은
  `idempotency_key_conflict`, `retryable=false`, `queue_action=discard`로 반환한다.
- `commit_state=unknown`과 request-context가 끊긴 처리 결과는 raw 재실행이
  안전하다고 증명될 때까지 terminal reconciliation 상태로 ledger에 고정한다.
  진행 중 owner 수는 bounded capacity로 제한하며, owner가 뒤늦게 실제 결과를
  반환하면 같은 entry의 provisional unknown 응답을 실제 응답으로 교체한다.
- source acceptance는 idempotent write closure 안에서 한 번만 실행한다.
  같은 accepted final의 중복 POST가 acceptance ledger와 raw save를 다시
  진행하지 않는다.
- logical replacement 오류는 `code`, `stage`, `retryable`,
  `commit_state`, `raw_committed`를 가진 typed error로 반환한다. revision/tail,
  권한, 중복, FK constraint, lock wait/deadlock, commit outcome unknown을
  구분한다. raw replacement commit 뒤의
  reference/narrative cleanup 실패는 `status=partial`, `save_ok=true`,
  `reconciliation_required=true`, `queue_action=discard`로 반환하여 raw를
  재전송하지 않는다.
- request-status도 `save_ok`, `raw_committed`, `commit_state`,
  `derived_retry_required`, `reconciliation_required`를 보존한다. polling
  경로가 partial success를 단순 `ok`로 축약하지 않는다.
- `raw saved + derived retry required`는 critic 실패뿐 아니라 일반
  derived/audit persistence 실패에서도 complete-turn 전체 실패가 아니다.
  응답은 `status=partial`, `save_ok=true`, `raw_committed=true`,
  `derived_retry_required=true`로 raw와 파생 상태를 분리한다. backend가 파생
  재처리·reconciliation을 소유하며 JavaScript failed queue에는 raw save
  재시도 항목을 남기지 않는다.
- `verify_unavailable`과 `raw_chat_log_verify_unavailable`은 optional read-back
  증거 부재다. `save_ok=true`를 실패나 경고 저장 상태로 뒤집지 않는다.

#### 검증 상태

- 아래 항목은 2026-07-30 통합 시험에서 다시 통과시켜야 하는 source gate다.
  과거 통과 기록만으로 현재 수정본을 완료 처리하지 않는다.
- Go `./...` 전체 테스트
- JavaScript route/smoke 전체 테스트와 Node syntax check
- hidden finality 상수, timer polling, synthetic watcher 재진입의 production
  source 부재
- append와 same-length reroll의 exact slot, host ID/content equality,
  superseded await race, concurrent drain lost-wake, duplicate accepted hook을
  포함해 exact host signal이 accepted-final continuation을 한 번만 실행하는
  fixture 통과
- copied chat tail deletion, reroll/source acceptance, request status,
  full semantic idempotency fingerprint conflict와 logical replacement
  permission/deadlock/precommit/commit-unknown/postcommit fixture 통과
- pending payload의 credential-free 별도 영속·reload 복구, 동일 observation
  거부, live critic/embedding 재결합, request-status rejected/raw-saved 분리,
  raw-saved/derived-audit-failed 중복 POST 방지 fixture 통과
- failed queue의 stable semantic fingerprint, 같은 key의 payload 순서 차이,
  capacity overflow 무축출, terminal reload, superseded pending 미복원 회귀 통과
- pending recovery와 failed queue의 2단계 durable transition intent 회귀 통과
- intent 첫 쓰기 실패 뒤 일반 저장만 성공한 경우도 typed terminal
  `reason_code`·`terminal_at`으로 복원되고 reconciliation 전 POST가 0회인
  재기동 회귀 통과

이는 source-level 검증이다. 실제 RisuAI에서 non-streaming, long streaming,
DeepSeek, GLM, reroll, delete, `/cut`, copy/fork, plugin reload를 거친
loaded-artifact 검증은 아직 release gate로 남는다.

pending confirmation의 closure 자체는 브라우저 메모리에만 존재하지만,
credential-free payload와 관측 fence는 별도 pluginStorage에서 복원한다.
재시작 후에도 현재 RisuAI active chat의 정확한 final 관측 전에는 POST하지
않는다. 이미 저장 완료된 항목은 backend idempotency status가 중복 raw write를
차단하고, active-chat backfill도 같은 source acceptance 계약을 거친다.

#### 2026-07-30 live-host 감시 hotfix

- Archive Center를 활성화했을 때 RisuAI `SafeMutationObserver`에서
  `e.getAttribute is not a function`이 재현됐다. 원인은 finality 확인을 위해
  root document 전체에 등록한 영구 `MutationObserver`와
  `characterData: true`였다. character-data mutation의 target은 `Text`인데,
  pinned RisuAI 구현은 모든 target을 `SafeElement`로 감싸며 곧바로
  `getAttribute()`를 호출한다.
- `characterData` 옵션만 빼는 축소 대응이 아니라 finality용 전역 observer,
  상태 필드와 초기화 호출을 모두 제거했다. 다음 `input`/`beforeRequest` 외에는
  active chat backfill을 깨우는 별도 timer·DOM 신호가 없다.
- HUD 상태 구독은 실제 prepare/complete 요청에서만 시작하며
  `/turn-workflow/events` NDJSON 단일 연결로 revision을 순서대로 받는다. 연결의
  종료는 backend terminal, RisuAI 요청 취소 또는 실제 transport 단절뿐이다.
  UI request timeout을 `wait_ms`로 stream에 전달해 Critic 처리 중 nonterminal EOF를
  만들던 경로는 제거했다.
  스트림 body를 지원하지 않는 Host는 `stream_transport_unavailable`을 표시하고
  polling fallback을 시작하지 않는다.
- 이 hotfix는 source 회귀와 구문 검사를 통과해야 하며, loaded RisuAI artifact에서
  플러그인 활성화·일반 출력·긴 스트리밍·리롤을 다시 확인하기 전까지 live 완료로
  판정하지 않는다.

### 3.7-C — Critic과 파생 기억 복구

#### 처리한 사용자 피드백

3.7-C는 다음 현상에 대응한다.

- `CRITIC_LLM_FAILED` 하나만 보여 provider 호출 실패, timeout, 빈 응답,
  JSON 누락·문법 오류와 schema 오류를 구분할 수 없던 문제
- OpenAI 형식에서는 정상인데 Vertex/Gemini 형식에서는 실패하거나
  `invalid character ... after array element`, `critic_json_missing`이 반복되던 문제
- raw 원문은 저장됐지만 평론가 파생 기억이 생성되지 않고 실패 큐만 늘어나던 문제
- retryable Critic 작업이 15분 backoff에 도달한 뒤에도 최대 횟수 없이 영구 반복되어
  호출 비용과 실패 큐가 계속 누적될 수 있던 문제
- 평론가가 잘못 만든 비밀·NPC 주관 기억이 protected memory로 승격되어 이후 입력에
  과도한 guard를 붙일 수 있던 문제
- 리롤·삭제 뒤 이전 source revision의 늦은 평론가 결과가 identity, surface,
  speaker 또는 파생 기억으로 남을 수 있던 경합
- `maintenance_enqueued`, `queue_depth=1`, `drift_detected`가 실제 worker 작업처럼
  보이지만 실제로는 audit shadow였던 표시 문제

#### 구현된 계약

1. Critic 실패를 구조화했다.
   - `CRITIC_PROVIDER_TIMEOUT`
   - `CRITIC_PROVIDER_CANCELED`
   - `CRITIC_PROVIDER_CALL_FAILED`
   - `CRITIC_PROVIDER_HTTP_ERROR`
   - `CRITIC_EMPTY_RESPONSE`
   - `CRITIC_CONFIG_INVALID`
   - `CRITIC_REQUEST_BUILD_FAILED`
   - `CRITIC_JSON_MISSING`
   - `CRITIC_JSON_PARSE_FAILED`
   - `CRITIC_SCHEMA_INVALID`
   - configuration/input 오류도 각각 별도 코드로 유지한다.
   각 오류는 `stage`, `retryable`, 가능한 경우 `http_status`를 가진다.
2. 실제 upstream HTTP 오류와 호출 전 configuration/request-build 오류를 분리한다.
   Claude·Gemini·Vertex의 빈 2xx 응답도 가짜 502로 바꾸지 않고 실제 상태와
   `CRITIC_EMPTY_RESPONSE`를 보존한다.
3. provider·parse·schema 실패 trace에 provider, model, HTTP status와 길이가 제한된
   `raw_preview`를 보존한다. configured API key뿐 아니라 Authorization/Bearer,
   API key, password와 access/refresh token 값 전체를 제거한다. complete-turn 감사
   기록과 durable reprocessing worker가 같은 분류기를 사용하며, worker 감사에는
   job ID, source revision, turn, attempt도 함께 남긴다.
4. JSON object가 비어 있거나 알려진 extraction payload가 없거나 주요 필드 자료형이
   잘못되면 normalization 전에 schema 오류로 중단한다.
5. protected secret, protected/NPC subjective memory와 protected identity 후보는
   owner·본문·명시적 reveal policy·현재 source에 정확히 존재하는 evidence를 모두
   만족해야 한다. evidence가 원문에 존재하더라도 보호 기억의 주장과 핵심 anchor가
   연결되지 않으면 격리한다. 하나라도 없으면 저장·주입하지 않고 quarantine reason
   count만 trace에 남긴다. subjective memory 저장 직전에도 같은 fail-closed 검사를
   다시 한다.
6. durable critic job은 exact `source_revision`에 연결된다. 실행 중 reroll·삭제가
   확인되면 provider/save context를 취소하고 결과를
   `CRITIC_RESULT_SUPERSEDED`, `stale_rejected`, terminal로 끝낸다.
7. identity, identity surface, artifact binding과 speaker attribution의
   accepted-source MariaDB 쓰기는 active source row를 `FOR UPDATE`로 잠근 같은
   transaction 안에서 검사하고 저장한다. foreground 재확인만으로 남던 TOCTOU를
   store 경계에서 닫는다.
8. 실제 durable worker queue는 `memory_reprocessing_jobs` 하나로 표시한다.
   maintenance handoff는 `maintenance_audit_recorded=true`,
   `maintenance_enqueued=false`, `queue_depth=0`, `queue_mode=none`,
   `audit_mode=plan_only`로 기록한다.
9. durable Critic retry의 최대 횟수는 새 고정값을 만들지 않고 UI의 기존
   `failedQueueMaxAttempts`를 backend 설정으로 전달해 1~11 범위에서 사용한다.
   설정이 전달되지 않았거나 유효하지 않으면 `CRITIC_RETRY_LIMIT_UNCONFIGURED`,
   한도에 도달하면 `CRITIC_RETRY_LIMIT_REACHED`로 terminal 처리한다.
10. Critic pipeline을 `critic_pipeline.v2`로 올려 새 결과가 이전 파서 결과와 같은
   idempotency 세대로 오인되지 않게 했다.
11. rescan의 duplicate enqueue는 더 이상 성공으로 집계하지 않는다. 명시적
   `force_derived_rebuild`에서만 exact active source와 기존 job을 transaction으로
   잠그고 admission snapshot/job을 다시 연다. raw source와 기존 secondary row는
   보존하며, `reopened` 응답과 `memory_reprocessing_reopened` 감사를 남긴다.

기존 오염 데이터의 운영 절차는
[`3.7-critic-derived-memory-reprocessing-runbook.md`](3.7-critic-derived-memory-reprocessing-runbook.md)에
분리했다. raw chat과 effective input은 삭제하지 않으며, 재스캔만으로 이미 남아 있는
모든 잘못된 secondary row가 자동 제거된다고 주장하지 않는다.

#### 검증 상태

- `go test ./... -count=1`: 통과
- provider timeout·HTTP 상태·빈 응답·JSON parse·schema 분류 회귀: 통과
- local config/request-build와 실제 upstream HTTP 오류 분리 회귀: 통과
- credential value redaction과 bounded raw preview 회귀: 통과
- malformed·claim-unbound protected candidate quarantine와 저장 직전 방어 회귀: 통과
- provider 응답을 지연시킨 뒤 source를 supersede한 worker 경합 회귀: 통과
- accepted-source identity writer의 active/stale/legacy MariaDB 계약 회귀: 통과
- maintenance audit와 실제 worker queue 표시 분리 회귀: 통과
- UI-configured Critic 최대 재시도와 worker 실패 감사 회귀: 통과
- duplicate rescan skip와 exact force reopen MariaDB 회귀: 통과
- bundled Node `Archive Center.js --check`, `git diff --check`: 통과

이는 source-level 검증이다. 실제 OpenAI·Vertex/Gemini·Claude provider 호출,
실 MariaDB lock 경합, RisuAI reroll/delete와 packaged worker 재시작 검증은
loaded-artifact·live release gate로 남는다.

### 3.7-D — 세션·DB·Vector·업데이터

#### 처리한 사용자 피드백

3.7-D는 다음 현상과 위험에 대응한다.

- 일부 session table만 복사한 뒤 원본 cleanup이 가능해 다른 세션의 기억·관계·상태가
  유실될 수 있던 문제
- character/chat index 이동, copy/fork, reload 뒤 서로 다른 채팅이 같은 SID로
  묶이거나 pin 저장 실패가 성공처럼 보일 수 있던 문제
- Windows에서 ChromaDB가 설치되었어도 endpoint와 실제 upsert/readback이 확인되지
  않아 매 턴 `vector_not_configured`가 반복되던 문제
- 기존 MariaDB 계정이 연결은 되지만 CRUD 또는 DDL 권한이 부족해 저장·migration
  단계에서 뒤늦게 실패하던 문제
- 3.0/3.5 updater가 `status` 없는 응답, 섞인 stdout/stderr, 불명확한 recovery
  runner 또는 변경된 migration을 만나 mixed package 방지로 기동을 중단하던 문제
- updater 파일명을 바꾸어 검사를 우회하면 기동은 되지만 pending transaction의
  주체와 data root가 검증되지 않던 운영상 우회

#### 구현된 계약

1. `session-migration.manifest.v1`은 `001_schema.sql`의 session-content table을
   전수 검사해 직접 table 46개와 간접 child 4개를 분류한다.
   `session_fork_lineage.copied_from_session_id`처럼 보조 SID column도 별도 의미를
   기록한다. migration ledger, lock, route binding은 content manifest에서 제외한
   metadata로 명시한다.
2. table별 copy/retain-audit/regenerate/delete-after-verified 정책과 구현 여부를
   기록한다. 현재 executor가 다루지 못하는 항목이 남아 있으므로
   `session_migration_manifest_parity_unverified`,
   `source_target_count_hash_unverified`, `row_map_fk_remap_unverified`,
   `vector_expected_id_parity_unverified`를 release blocker로 반환한다.
3. count/hash, row-map/FK와 vector expected-ID parity를 저장하는 ledger,
   recoverable saga step, durable route binding table을 fresh schema와 additive
   `007_session_migration_manifest_and_route_binding.sql`에 함께 추가했다.
   manifest executor와 parity가 완성되기 전에는 source lock과 cleanup을 계속
   fail-closed로 막고, reindex도 `vector_verified_manifest_blocked`에서 끝낸다.
4. 공식 host에서 관측한 stable `character.chaId`와 opaque `chat.id`의 조합을
   backend durable binding의 unique identity로 사용한다. 전체 ID 조합을 비교하며
   MariaDB serializable transaction 안에서 exact readback을 확인한 canonical SID만
   반환한다. locked source는 검증된 target으로 redirect한다.
5. JavaScript pin key와 record는 v3로 통일했다. 구 index 기반 v1 pin은
   `legacy_promotion` 입력으로만 사용한다. stable identity가 관측된 경우 backend
   binding readback 또는 local pin exact readback이 실패하면 cache 갱신, `/turns`
   저장 요청과 retry queue 진입을 모두 중단한다. 수동 attach/migration UI도
   acknowledgement가 없으면 성공을 표시하지 않는다.
6. Windows dependency probe는 명시적 `-Execute`가 있을 때만 고유 임시
   collection/table을 만든다. Chroma는 production store로
   `Upsert → Get → QueryExact → Count → Delete → Count(0) → ResetAll`을 검사한다.
   Chroma v2가 collection ID DELETE에 성공을 반환하면서 실제 collection을 남기는
   동작을 실환경에서 재현해 name DELETE와 GET 404 사후검증으로 수정했다.
   DELETE 자체가 404여도 named GET으로 실제 부재를 확인하며, fallback DELETE가
   404인데 collection이 남아 있으면 성공으로 처리하지 않는다.
7. MariaDB application account probe는 non-root local TCP 계정만 허용하고 입력
   DSN으로부터 `127.0.0.1`과 `localhost` 두 target을 만든다. 각 target에서
   `PING → CREATE → INSERT/SELECT → UPDATE/readback → DELETE → ALTER → DROP`을
   schema 적용 전에 수행해 권한 부족 환경을 부분 schema 변경보다 먼저 차단한다.
   DSN과 password는 report에 넣지 않으며 권한 오류는
   `DB_PERMISSION_DENIED`와 stage/operation으로 반환한다.
8. Windows updater IPC는 stdout/stderr를 분리하고 contract, action, action별
   status, exit code와 failure code를 정확히 검사한다. update state는 preserved
   runner path와 SHA-256을 묶어 recovery 때 가장 최신 파일을 추측하지 않는다.
   committed state 뒤 같은 pending marker가 남은 crash window는 재적용하지 않고
   idempotent cleanup한다.
9. migration inventory가 달라지는 package는
   `archive-center.package-migration-update.v1` 계약이 필요하다. 이 계약은 package
   manifest가 인증하며 source version별 정확한 기존 migration size/hash와 target
   inventory를 비교한다. 검토되지 않은 임의 SQL을 문자열 규칙으로 자동 승인하지
   않는다.
10. 3.0.0·3.0.1·3.5.0 Windows update package에는 별도
    `archive-center.external-update-bridge.v1` compatibility bridge를 제공한다.
    bridge는 자체 script·updater·선택한 update ZIP의 SHA-256과 실제 공개 package의
    migration fingerprint를 검사한 뒤 새 updater를 보존 runner로 먼저 실행한다.
    3.0.0·3.0.1에서만 필요한 11개 obsolete managed path는 source fingerprint
    계약에 정확히 열거된 경우에만 transaction journal로 제거하며 rollback 때
    원본 바이트를 복원한다. 임의 경로 삭제 API는 열지 않는다. bridge 성공은
    `applied_pending_health`이며 launcher health commit 전에는 완료로 표시하지 않는다.
11. Windows launcher와 external installer는 package 폴더가 아닌
    `ARCHIVE_CENTER_DATA_DIR` 또는 `%LOCALAPPDATA%\ArchiveCenter\data`를 stable data
    root로 사용한다. legacy data import는 source를 지우지 않고 count/hash 검증과
    resumable marker를 사용한다. MariaDB port/process 또는 기본·설정된 local
    Chroma endpoint가 살아 있으면 raw data directory copy를 거부한다.
    Chroma guard는 `127.0.0.1`과 `::1`, 설정된 local endpoint를 해당 IP 주소군으로
    직접 검사한다. source를 고유 staging directory에 복사·검증한 뒤 같은
    volume에서 destination으로 promote하며, 성공 marker는 promote가 끝난 뒤에만
    기록한다.

#### 검증 상태

- `go test ./... -count=1`: 통과
- bundled Node `Archive Center.js --check`: 통과
- 변경된 PowerShell 7개 parser 검사: 통과
- stable binding/pin 실패 때 cache·backend payload·retry queue가 모두 변하지 않는
  production JavaScript smoke: 통과
- 현재 PC의 source-driven Windows dependency live probe:
  - Chroma `127.0.0.1:8000` upsert/get/query/count/delete/count-zero/collection
    cleanup: 통과
  - 남은 `archive_center_windows_probe_*` collection: 0
  - 해당 listener는 기존 3.5 테스트 패키지의 Python/ChromaDB 1.5.9였다.
    `%LOCALAPPDATA%\ArchiveCenter\runtime`에는 이 검증 시점에 관리형 Python이
    없었으므로 새 3.7 관리형 Chroma 설치·기동 증거로 간주하지 않는다.
  - MariaDB 기존 non-root application account의 `127.0.0.1`과 `localhost`
    CRUD·CREATE/ALTER/DROP 및 cleanup: 통과
- 최종 source에서 managed Windows full ZIP과 update ZIP 생성: 통과
- update package migration contract:
  - target migration 7개와 `007` 포함
  - source inventory `3.0.0`, `3.0.1`, `3.5.0` 포함
  - Git blob이 아닌 실제 공개 Windows Update ZIP의 migration 바이트 사용
  - package manifest 인증 확인
- production updater CLI E2E:
  apply/status/commit, explicit rollback, interrupted apply recovery,
  readiness-failure rollback, tampered archive mutation 전 거부: 통과
- external compatibility bridge E2E:
  - 공개 v3.0.0 ZIP SHA-256
    `3aad97de494821f79509ec34c42b3b0854b32e7699afa04ef4d7846fd6e3006f`
  - 공개 v3.0.1 ZIP SHA-256
    `5611d61856eae8ca8513bc723cf616d1e312bcc23782cb0d534450814ab08b7e`
  - 공개 v3.5.0 ZIP SHA-256
    `eb44280a35bd4d3ca594ee5cf8e7887f0f3f3ee7faa548b849fbd7d9f063acd0`
  - 세 공개 package에서 candidate 적용, `applied_pending_health`, 전체 관리 파일
    byte-for-byte rollback 복원: 통과
- launcher와 external installer의 legacy Chroma import smoke:
  IPv4 `127.0.0.1`과 IPv6 `::1` live endpoint 거부,
  destination/marker 미생성, endpoint 종료 후 staging 검증·atomic promote,
  source 보존, staging cleanup: 통과
- 최종 IPv6·compatibility bridge 보완 뒤 Windows managed full ZIP과 update ZIP,
  bridge ZIP을 다시 생성하고
  updater CLI E2E와 민감파일 hygiene, migration contract hash 인증을 재검사:
  통과
- fresh package `core_lite/fallback` shadow smoke와 `/ready=true`: 통과
- JavaScript 증감: `+246 / -53`. 추가된 로직은 stable host identity 관측,
  backend transport, pin readback과 UI acknowledgement에 한정한다.
- 50/50 manifest·installed schema·row-map·FK·semantic reference·9개 Vector
  tier 집중 회귀: 통과
- provisional pending lock의 no-redirect, source/target range `FOR UPDATE`,
  exact/subset Vector parity, one-shot proof 재사용 차단, post-vector resume,
  cleanup 중 source·target·Vector drift 회귀: 통과
- `go test ./internal/store ./internal/httpapi ./internal/vector -count=1`: 통과

이번 source-live MariaDB probe는 현재 로컬 DB에 additive fresh/compatibility
schema를 적용했으며 3.7-D ledger/binding table을 `CREATE IF NOT EXISTS`로 만들었다.
사용자 chat/raw/derived row는 삭제하지 않았고 두 임시 권한 probe table은 모두
DROP했다. Chroma 임시 collection도 모두 제거했다.

#### 3.7-D 전체 manifest executor source closure

pre-G 재감사에서 기존 8-table executor를 제거하고 다음 source 계약을 추가했다.

- 50개 manifest entry 전부 static execution plan을 가지며 fresh schema의
  column order, PK order, generated column, session-leading range index와
  일치하지 않으면 target mutation 전에 차단한다.
- 숫자·UUID·revision key를 universal row-map으로 변환하고 SQL FK뿐 아니라
  source revision, direct-evidence ID 배열, derivation root/child/parent 같은
  semantic reference도 재매핑·검증한다.
- Vector는 memory, evidence, world rule, KG, episode, chapter, arc, saga,
  precise memory의 9개 tier에서 canonical ID·text·eligibility와 expected-ID
  parity를 검사한다.
- source lock 전에는 exact target Vector 집합을 요구하고, cutover 이후 cleanup은
  이관된 ID가 모두 남아 있는지만 검사해 정상적인 신규 target 자료를 허용한다.
- source lock은 provisional `lock_pending_verification`을 먼저 영속화해 새 쓰기와
  redirect를 막는다. 최종 검증은 `REPEATABLE READ`와 manifest range
  `FOR UPDATE`로 이미 진행 중인 MariaDB writer를 경계 안에 둔다.
- Vector의 List→proof→lock/resume/cleanup은 managed single-process
  `mutationFencedStore`의 exclusive callback 안에서 실행한다. TTL은 사용하지
  않고 operation-scoped proof를 한 번만 소비한다.
- cleanup은 durable prepare, source Vector 삭제, 최종 current-state proof,
  MariaDB finalize 순서이며 중간 실패는 source DB 행을 보존한 복구 상태로 남긴다.

다음 항목은 아직 완료로 주장하지 않는다.

- packaged `full_local` Windows launcher가 관리형 MariaDB/Chroma를 직접
  설치·기동한 상태의 loaded-artifact roundtrip
- 구형 updater binary 자체가 새 migration을 직접 승인하는 동작은 바뀌지 않는다.
  지원 경로는 update ZIP과 함께 배포하는 인증된 compatibility bridge를 사용자가
  별도로 실행해 새 updater를 먼저 적용하는 방식이다.
- compatibility bridge 이후 실제 packaged launcher의 `/ready`·`/version` 확인과
  자동 commit 또는 health-failure rollback
- external installer의 실제 network download 및 live legacy data import/recovery
- POSIX builder의 새 migration update contract 생성
- 실제 RisuAI copy/fork/index 이동/reload와 수동 attach loaded-plugin 검증

따라서 3.0.0·3.0.1·3.5.0 공개 Windows package에서 3.7 candidate까지의
authenticated bridge와 rollback compatibility는 source/package E2E로 닫혔다.
다만 실제 설치 PC에서 bridge 이후 packaged launcher가 backend health를 확인해
commit하거나 자동 rollback하는 loaded-artifact gate는 아직 남는다. 구 updater
검사를 우회하거나 `applied_pending_health`를 완료로 바꾸어 표시하지 않는다.

#### 3.7-D 종료 전 재검증 보완

3.7-E를 시작하기 전에 `4756289`의 3.7-D 구현을 독립 재검증했고, 다음 세 경계를
추가로 닫아 `8f5b86f`에 반영했다.

1. migration manifest parity가 미완료일 때 target table이나 Vector collection에
   먼저 쓰고 나중에 실패하던 순서를 바꿨다. 현재 50/50 executor도 필수
   schema·manifest blocker를 target mutation 전에 검사한다.
2. 필수 Vector가 준비되지 않은 상태에서는 `/ready=true`를 반환하지 않는다.
   updater health commit이 MariaDB만 정상인 혼합 상태를 성공으로 승인할 수 없게
   했다.
3. Windows 설치·복구 검증에서 MariaDB CRUD·DDL과 Chroma roundtrip을 확인하는
   진단 도구를 추가했다. 이후 재감사에서 이를 매 시작마다 실행하는 것은 런타임
   동작을 바꾸고 Chroma 응답 정지 시 시작을 무기한 막을 수 있어 제거했다.
   launcher는 MariaDB 계정 bootstrap·schema 적용과 Chroma 설치·기동·endpoint
   준비를 수행하고, destructive test record 왕복은 배포 런타임이 아닌 명시적
   설치/검증 단계에서만 수행한다.

관련 Go 회귀는 통과했다. 다만 **3.7 schema를 실제 구형 DB에 적용한 뒤 구형
backend binary를 다시 실행하는 schema-forward compatibility live test**는 아직
없다. 기존 updater E2E는 3.0.0·3.0.1·3.5.0 package의 파일 byte/hash 복구를
증명하지만, 이 DB runtime 증거를 대신하지 않는다. 이 항목은 3.7-D의 남은
loaded-artifact release gate로 유지한다.

### 3.7-E — HUD와 대시보드 사실성

#### 처리한 사용자 피드백

3.7-E는 다음 현상과 오해에 직접 대응한다.

- `waiting for RisuAI active chat confirmation`이 실패 큐와 같은 경고로 보이고,
  30분 전 또는 다른 세션의 실패 항목이 현재 턴 문제처럼 나타나던 현상
- `raw:ok`, `derived:ok`, `vector_not_configured`, `critic_extract_failed`처럼
  서로 다른 저장 lane의 상태가 하나의 성공·경고 문구로 뭉쳐지던 현상
- HUD에 6/12 같은 단계만 보여 실제 Host 관찰, backend 처리, payload 전달,
  원문 저장, 파생 기억과 Vector 중 어디에서 멈췄는지 알기 어려웠던 현상
- 현재 Host turn과 backend turn이 다르거나 Dry Run이 과거 turn을 보아도
  불일치를 한 화면에서 확인할 수 없던 현상
- OOC·삭제·리롤·정확한 중복 재전송 같은 정상적인 운영 이벤트까지 경고로
  보이거나, OOC 표시를 JavaScript가 backend 응답처럼 자체 조립하던 문제
- warning/error 창이 카드 바깥 또는 카드 본문 클릭으로 닫혀 원인을 읽기
  어려웠던 문제

#### 구현된 계약

1. HUD 계약을 `turn_workflow_hud.v2`로 올리고 다음 typed fact를 추가했다.
   각 fact는 `owner`, `scope`, `status`, `disposition`, `reason_code`,
   `severity`, 선택적 `count`를 가진다.
   - `host_observation`
   - `backend_processing`
   - `context_selection`
   - `payload_delivery`
   - `finality`
   - `raw_persistence`
   - `derived_memory`
   - `vector_index`
2. disposition은 `eligible`, `selected`, `delivered`, `deferred`, `dropped`만
   허용한다. 잘못된 값은 성공처럼 표시하지 않고 `deferred`와 typed reason으로
   정규화한다.
3. prepare 단계에서 RisuAI source observation, backend context selection과
   Host payload 적용 대기를 분리한다. complete 단계에서는
   `source_to_final_lineage_observation`이 `archive_center_before_request_return`에서
   `applied` 또는 `empty`였을 때만 payload를 `delivered`로 표시한다.
   active-final acceptance와 payload 전달을 같은 사실로 합치지 않는다.
4. complete-turn의 실제 persistence pipeline 결과를 raw, derived, Vector fact에
   각각 연결한다. 저장 수와 `ok`, `empty`, `queued`, `skipped`,
   `vector_not_configured`, `error`를 유지하며, Vector 실패가 raw 저장 성공을
   실패로 바꾸지 않는다.
5. 전체 severity는 `normal`, `notice`, `warning`, `error` 네 단계다.
   정상·알림은 카드 또는 X로 닫을 수 있고 warning·error는 backend가
   `dismissal_policy=x_only`로 반환한다. JavaScript는 이 계약을 그대로 렌더링하며
   자체적으로 severity나 닫기 정책을 재판정하지 않는다.
6. 성공한 backend/finality fact는 다른 lane의 Vector 경고를 상속하지 않는다.
   전체 카드는 Vector 경고를 유지하되 개별 backend/finality fact는 성공으로
   남는다. 반대로 `awaiting_risu_host_final`의 일시적인 notice는 정상 완료 뒤
   남지 않으며, 실제 deferred/dropped fact의 notice·warning은 유지한다.
7. 기존 OOC 판정 결과만 `turn_workflow_notice_observation.v1`로 Go에 전달한다.
   endpoint는 정확한 OOC observation만 허용하며, title·severity·dismissal과
   `turn_workflow_hud.v2` 응답은 Go가 만든다. JavaScript의 가짜 backend-shaped
   OOC ViewModel 조립은 제거했다. HUD 표시 설정을 꺼도 대시보드용 backend
   observation은 기록하되 화면에는 띄우지 않으며, transport 실패는 OOC 입력
   취소나 본문 응답을 실패시키지 않고 fail-open한다. 별도 5초 고정 제한 없이
   기존 사용자 요청 timeout 설정을 그대로 사용한다.
8. 삭제 성공은 notice, 삭제 동기화 부분 실패는 error, reroll 교체 성공은
   notice로 표시한다. 정확히 같은 중복 replay는 notice로, 내용 충돌 중복은
   warning으로 구분한다. 이 operation notice도 Go ledger에 기록해 대시보드가
   같은 사실을 읽을 수 있다.
9. `host_turn`, `backend_turn`, `turn_alignment`를 추가해 `aligned`,
   `host_ahead`, `backend_ahead`, `unobserved`를 reason과 함께 표시한다.
   HUD는 불일치일 때 `Host N / Backend M`을 함께 보여준다.
10. 대시보드를 `dashboard.viewmodel.v2`로 올리고 현재 session의 최신 Go workflow
    snapshot을 읽어 typed fact card를 만든다. 새 workflow card는 번역 문구를
    정규식으로 분석해 상태를 추측하지 않는다.
11. queue observation은 `transport_retry`, `pending_confirmation`,
    `maintenance`로 분리하고 `current_request`, `current_session_history`,
    `other_session`, `unknown` scope를 부여한다. request ID가 서로 다르면 같은
    session·logical turn이어도 과거 queue로 분류한다. 전체 실패 큐 depth만 있고
    correlation이 없으면 현재 경고가 아니라 `Historical Queue / unknown` notice다.
12. JavaScript는 RisuAI hook·session/message 관찰, 최소 queue correlation
    metadata 전송, DOM 렌더링만 담당한다. queue payload 본문과 API key는
    대시보드 요청에 넣지 않는다. 새 watcher, finality timer 또는 별도 상태
    캐시는 추가하지 않았다.
13. 대시보드 요청에 exact current workflow request ID가 있으면 같은 session의
    더 최신 operation notice로 바꾸지 않는다. exact ID가 없을 때만 session
    최신 snapshot을 사용하므로 현재 요청과 직전 삭제·중복 알림이 섞이지 않는다.
14. maintenance queue 항목은 enqueue 응답에 실제로 들어 있던 session,
    request ID, logical turn만 사용한다. correlation이 없는 maintenance audit를
    현재 요청으로 합성하지 않으며, maintenance audit 저장 실패도 derived memory
    저장 실패로 바꾸지 않는다.
15. OOC와 rollback operation request ID를 session·turn·operation source로
    결정적으로 만들고 Go ledger에서 같은 ID·같은 notice를 idempotent하게
    재사용한다. 같은 Host hook이나 transport 재시도가 operation card를 여러 개
    만들지 않는다.
16. rollback HUD의 raw, derived, Vector fact는 실제 `deletions` 결과에서 만든다.
    source invalidation 단계에서 중단되면 세 lane과 finality를 `blocked/error`로
    표시하고, 정상 삭제·부분 실패·Vector 비설정·durable outbox 재시도를 각각
    `deleted`, `partial_error`, `skipped`, `queued`로 구분한다. Vector outbox의
    permanent 실패는 재시도 가능으로 쓰지 않고 canonical 삭제 성공과 Vector
    영구 실패를 분리해 top-level delete sync `partial_error`로 올린다.
17. reroll로 superseded된 기존 workflow는 `accepted`로 남기지 않고 finality를
    `invalidated/dropped`로 갱신한다. 새 active-final과 이전 revision의 성공
    표시가 동시에 current truth처럼 보이지 않는다.

#### 검증 상태

- bundled Node를 PATH에 연결한 `go test ./... -count=1`: 전체 package 통과
- 현재 최종 `Archive Center.js`의 bundled Node `--check`: 통과
- HUD DOM runtime, typed fact·turn mismatch, OOC backend notice와 confirmed
  reroll adapter fixture: 전체 Go package 회귀 안에서 통과
- typed fact·persistence·turn alignment·dismissal 회귀: 통과
- OOC endpoint allowlist와 production complete OOC 응답 회귀: 통과
- HUD 비활성 상태의 OOC backend 관찰 유지, 화면 미표시와 transport fail-open
  adapter 회귀: 통과
- exact duplicate replay notice와 content conflict warning 분리 회귀: 통과
- 동일 session·동일 turn의 과거 request ID가 current queue로 승격되지 않는
  대시보드 회귀: 통과
- exact current workflow ID가 더 최신인 같은-session operation으로 치환되지 않는
  대시보드 회귀: 통과
- maintenance provenance 미확인 항목의 historical 분류와 maintenance audit 실패가
  derived persistence 실패로 승격되지 않는 회귀: 통과
- rollback 성공·source invalidation 차단·Vector outbox 재시도의 typed lane
  상태와 결정적 operation ID 회귀: 통과
- Vector outbox permanent 실패의 canonical/Vector 분리, top-level
  `partial_error`, error·X-only HUD 회귀: 통과
- JavaScript marker 회귀: 로컬 fake OOC ViewModel 부재와 typed endpoint 사용 통과

이는 source-level·fixture 검증이다. 실제 RisuAI/PocketRisu에서 streaming,
긴 reasoning, OOC, 삭제, reroll, duplicate, plugin reload를 수행한
loaded-artifact/mobile 검증은 아직 하지 않았다. warning/error X-only 동작도
실기기 확인 전에는 source 계약 통과로만 기록한다. dashboard current/history
queue는 terminal·retryable·unknown, `reason_code`, `terminal_at`을 typed
observation에서 보존하며 번역된 detail 문자열을 상태 권위로 사용하지 않는다.

### 3.7-F — 기억 전달과 서사 가이드 피드백

#### 처리한 사용자 피드백

3.7-F는 새 장기 기억 schema나 plot Director를 만드는 단계가 아니다. 현재
production prepare-turn 경로에서 이미 확인된 다음 불일치와 품질 저하를 닫는다.

- `compactPrepareTurnLine`이 전달받은 길이 제한을 적용하지 않아 compact trace가
  설정한 상한보다 길어지던 문제
- 설정의 `topK`가 backend에서는 Vector 후보 검색 상한인데 HUD·대시보드에서는
  최종 기억 총수처럼 보이던 문제
- Supervisor의 깨진 JSON, 올바른 빈 제안, 지원하지 않는 제안이 모두 정상 빈
  제안처럼 보이던 문제
- `romance`, `battle`, `kiss` 같은 영어 단어로 `auto` 장르를 판정해 입력 언어와
  표현에 따라 서사 모드가 달라지던 문제
- 서사 모드를 JavaScript 설정 화면에서 고를 수 없고 저장값을 다시 `auto`로
  덮어 실제 backend 동작과 UI가 달랐던 문제
- Go가 최종 선택·제외한 기억과 이유가 HUD에 연결되지 않아 “몇 개가 왜
  전달되었는지” 확인할 수 없던 문제
- Persona·hierarchy가 현재 요청과 무관해도 빈 수를 채우거나, 주관 기억이
  owner당 한 건으로 영구 차단되어 관련된 후속 기억도 사용할 수 없던 문제

#### 구현된 기억 전달 계약

1. 기존 `top_k`는 `vector_memory_search_limit_only`로 고정했다. protected guard를
   Vector 결과 수에 포함시켜 실제 관련 기억 후보를 밀어내지 않으며, 설정·HUD도
   이를 “Vector 후보”로 표시한다.
2. 별도 선택 설정 `core_objective_memory_max_items`와
   `core_objective_memory_delivery.v1`을 추가했다. 이 값은 relevance, entity
   coverage와 우선순위가 더 높은 직접 근거 lane과의 교차 중복 제거가 끝난
   객관적 사건 요약에만 최종 최대 상한으로 적용한다. 직접 근거로 이미 전달된
   중복 항목은 K를 소비하지 않으며, 문자 예산에 들어가지 못한 항목도 K를
   소비하지 않아 뒤의 서로 다른 관련 항목이 시도될 수 있다. 요청에서 필드가
   없으면 기존 item-count 동작을 유지한다.
3. 직접 근거, protected secret guard, 주관·관계 기억, 인물·세계·필수 상태,
   미해결 목표와 hierarchy support는 핵심 객관 기억 K를 소비하지 않는다.
   이는 무제한 주입을 뜻하지 않는다. 모든 lane은 기존 provenance,
   source lifecycle, visibility·privacy, 중복 제거, lane 문자 예산과 전체
   `max_injection_chars`를 계속 통과해야 한다.
4. 최종 lineage는 `eligible_distinct_count`, `candidate_count`,
   `delivered_count`, `deferred_by_limit_count`,
   `deferred_by_budget_count`, `missing_to_limit`, `gap_reason`을 분리한다.
   관련 기억이 K보다 적을 때 무관 기억을 채우지 않으며 `garbage_fill=false`,
   `top_k_reinterpreted=false`를 명시한다.
5. 최종 예산 조립 후 lineage의 delivered/deferred와 issue code를 다시
   계산한다. protected guard가 객관 기억보다 많다는 이유만으로 과거
   `protected_guard_dominates_final_memory_lines` 경고를 남기지 않고, 실제 객관
   기억이 완전히 전달되지 않은 경우에만 `actual_memory_absent`를 유지한다.
6. Persona는 현재 사용자 요청과 직전 accepted assistant 장면에 기억 내용,
   evidence 또는 owner가 아닌 semantic tag가 실제로 겹치는 항목만 선택한다.
   `owner_*`, `raw_owner_*`, visibility·source·reveal policy 같은 저장용 구조
   tag나 owner 이름 일치만으로 무관한 Persona 기억을 통과시키지 않고,
   내용 관련성을 통과한 뒤의 owner 일치는 정렬 가중치로만 사용한다. 한국어
   같은 비 ASCII 조사·활용이 붙은 표기도 보수적인 prefix 겹침으로 확인하되,
   단순 장기 세션이나 profile이라는 이유로 채우지 않는다.
7. hierarchy는 현재 요청과 관련된 가장 좁은 chapter→arc→saga 한 단계만
   선택한다. 장기 세션, 일반적인 “계속”, profile mode 같은 우회 조건으로
   무관한 상위 요약을 넣지 않는다.
8. 주관 기억은 privacy와 현재 입력 또는 관찰된 현재 장면의 owner scope를 먼저
   통과한 뒤, 각 기억의 text·evidence·tag가 현재 relevance context와 실제로
   겹치는지를 별도로 확인한다. owner 이름만 나온 경우 그 owner의 무관한 기억을
   전부 넣지 않는다. 이후 1차로 서로 다른 owner를 한 건씩 coverage하고, 남은
   문자 예산에서 같은 owner의 관련 비중복 항목을 추가한다. owner당 한 건이라는
   영구 hard cap과 수 채우기는 제거했다.

#### 구현된 서사 가이드 의미

1. `auto`는 본문 영어 키워드로 장르를 추정하지 않고 `standard`로 해석한다.
   `romantic`, `action`, `mature_*`는 사용자가 설정 UI에서 명시적으로 고른
   경우에만 적용된다. JavaScript는 선택값을 저장·전달하고 Go가 실제 모드와
   정책을 소유한다.
2. `none`은 Supervisor 호출과 output guidance만 0으로 만든다. 장기 기억,
   직접 근거, 비밀 보호, 상태와 continuity 주입은 계속 동작한다.
3. `weak`는 source-backed `response_focus`와 `must_account`,
   `medium`은 추가로 `may_advance` 또는 `hold_allowed`,
   `strong`은 추가로 `arc_anchor`와 `preferred_frontier`를 제안할 수 있다.
   이 이름들은 모두 선택형 표현 guidance이며 canonical 전이나 자동 scene
   transition 명령이 아니다.
4. 모든 강도에서 `truth_authority=false`, `canonical_write=false`,
   `force_progress=false`이며 사용자 행동, 새 진실, 관계 변화와 미해결 사건
   종결 권한을 차단한다. `strong`도 진행을 강제하지 않는다.
5. 새 complication을 생성하는 runtime 경로는 추가하지 않았다.
   `proactive_complication_default=off`,
   `proactive_complication_requires_opt_in=true`,
   `strong_implies_proactive=false`로 계약을 고정했다. 실제 생성 기능이 필요한
   경우 별도 명시적 opt-in, source constraint와 revision fence가 있는 후속
   계약 없이는 열지 않는다.
6. Supervisor JSON parse 실패는 `malformed_failed_open`, proposal envelope,
   list, item 또는 item 내부 `text`·`kind`·`source_refs` 타입이 계약과 다른
   응답은 `supervisor_schema_invalid`, 문법·schema상 올바른 빈 proposal은
   `valid_empty`, 허용하지 않는 kind·field만 있는 응답은
   `unsupported_rejected`, provider 실패는
   `supervisor_provider_failed_open`으로 분리한다. malformed 원문이나 stale
   제안은 prompt·trace·HUD에 재사용하지 않으며 본문 요청은 fail-open한다.
7. `compactPrepareTurnLine`은 공백 정규화 뒤 양수 길이 상한을 실제 적용하고,
   UTF-8 rune 경계를 보존하며 가능한 경우 단어 경계에서 자른다.

#### HUD·설정 연결과 비밀 경계

- Go `turn_workflow_memory_selection.v1` ViewModel이 Vector 후보 상한, 핵심 기억
  계약, lane별 eligible/selected/deferred/deduplicated 수, 최종 item disposition과
  typed reason, 제외 이유별 수를 제공한다.
- HUD는 Go ViewModel을 렌더링만 한다. delivered non-protected 항목에는 제한된
  preview를 표시할 수 있지만 protected text, 전달되지 않은 후보 text,
  `deferred_fact_keys`는 응답 ViewModel에서 제거한다.
- 설정 UI는 `핵심 연관 기억 최대 수`와 실제 `서사 가이드 모드`를 저장하고
  prepare-turn 요청에 전달한다. 강도가 `none`일 때만 모드 선택을 비활성화하며,
  저장값을 임의로 `auto`로 되돌리지 않는다.
- compact 응답의 검색 수, Supervisor 상태·실제 승인 지시 존재 여부와 호출 수는
  Go `prepare_turn.compact_orchestration.v1`이 계산한다. provider 실패를 설명하기
  위한 text 없는 trace item은 실제 directive로 세지 않는다. JavaScript는 이
  projection을 복사·표시할 뿐 기억 수나 Supervisor 상태를 다시 계산하지 않는다.
- plugin과 backend 기본 버전은 모두 `3.7.0-dev`로 맞추고, `guide_mode`의 frozen
  DTO/OpenAPI enum도 실제 UI·Go 계약인 `auto/off/standard/romantic/action/
  mature_soft/mature_direct`와 동기화했다.
- `Archive Center.js`에 추가된 동작은 RisuAI 설정 관찰·transport·DOM·현지화와
  Go trace 표시뿐이다. 기억 선택, K 적용, Supervisor 분류와 authority 판단은
  Go에 남는다.

#### 검증 상태와 남은 gate

- `go test ./internal/httpapi -count=1`: 통과
- bundled Node를 `ARCHIVE_CENTER_NODE_BINARY`로 지정한
  `go test ./cmd/js-route-variant-smoke -count=1`: 통과
- bundled Node `Archive Center.js --check`: 통과
- bundled Node와 로컬 Go cache를 지정한 `go test ./... -count=1`: 전체 통과
- compact 한글·emoji·단어 경계, 언어별 auto mode parity, malformed/empty/
  schema-invalid/unsupported Supervisor, provider 실패의 directive 오표시 방지,
  strength별 권한 차단, 교차 lane 중복 뒤 핵심 K와 support exemption,
  owner metadata-only Persona 차단, hierarchy·subjective relevance,
  HUD private text 비노출 회귀: 통과

이는 **source implementation과 자동 fixture 증거**다. 실제 loaded
`Archive Center.js`와 backend를 RisuAI에서 연결해 설정 저장·reload, 한국어
장면, protected memory, K 미달·초과, weak/medium/strong, malformed 실제 provider
응답과 HUD 표시를 확인하지 않았다. 따라서 상태는
`source_implemented / loaded-artifact guide-quality gate pending`이며 4.0의
정밀 recall·injection 완료나 proactive Director 완료로 표시하지 않는다.

### 3.7-G — 통합·실환경 검증과 배포

3.7-G는 A~F의 정책을 다시 설계하는 단계가 아니다. 현재 source를 실제 배포
artifact에 고정하고 Host·DB·Vector·updater·provider에서 같은 계약이 작동하는지
증명하는 단계다. source 시험 성공을 loaded artifact 성공으로 바꾸어 쓰지 않는다.

#### G-0 — 진입 전 정리

1. **완료:** failed `complete_turn` queue의 idempotency/generation/source
   revision 식별, duplicate/conflict, 무축출 overflow, durable terminal과
   reconciliation 계약을 source와 회귀로 닫았다.
2. **source 완료:** Windows/POSIX package builder, `.env.example`, Windows env
   예시의 기본 버전을 plugin·Go와 같은 `3.7.0-dev`로 동기화했다.
3. package manifest에 source commit, dirty state, 재현 가능한 build command,
   package version과 파일 manifest SHA-256을 기록한다.
4. loaded plugin build ID, backend `/version`, package manifest와 ZIP SHA-256을
   같은 evidence record에 연결한다.
5. **source 완료·실환경 gate 남음:** 전체 session migration의 50개 executor,
   count/hash/FK/semantic/vector parity와 동시성 fence는 구현됐다. 실제
   MariaDB·Chroma fault/recovery를 통과하기 전에는 배포 UI에서 실사용 가능으로
   승인하지 않는다.
6. **완료:** dashboard queue는 Go typed fact로 전환했고 current/history,
   retryable/terminal/unknown, reason/time을 문자열 판정 없이 표시한다.
7. proactive complication은 3.7에서 실제 기능이 아니라
   `default=off / explicit opt-in contract required`인 미래 경계로 고정한다.

#### G-1 — 자동·통합·fault 시험

- 전체 Go와 production JavaScript adapter 회귀
- migration manifest·binding·pin·cleanup fail-closed와 선택한 migration 제공 범위
- Critic 401/429/5xx, timeout, empty, malformed, schema-invalid, stale revision
- pending confirmation, transport retry, backend processing, terminal,
  superseded와 reload 복구
- MariaDB transaction·권한·중단 복구와 Chroma endpoint·outbox·readback 장애
- updater apply/status/commit/rollback, 중단 재개, tamper와 mixed-package 차단

#### G-2 — 실제 RisuAI lifecycle

- non-streaming과 streaming
- 긴 reasoning
- 번역/inlay가 있는 응답
- reroll·assistant delete·edit·branch·copy/fork·`/cut`
- plugin unload/reload와 backend restart
- desktop과 지원하는 mobile/remote 환경

각 lifecycle에서 Host candidate, accepted final, raw save는 정확히 한 번이어야 한다.
pending confirmation은 transport attempts를 소비하지 않아야 하며 reroll·delete된
revision의 늦은 결과는 저장·주입되지 않아야 한다.

현재 공식 RisuAI에는 post-commit output listener가 없다. 공식
`afterRequest`의 request-correlated v3 acceptance와 다음 `input`/`beforeRequest`
active-chat 복구 backfill을 각각 검증한다. DB post-commit 사실을 만들지 않으면서도
정상 응답을 다음 턴까지 보류하지 않는지 확인한다. DOM mutation, `display`,
고정 시간, synthetic `afterRequest`는 finality fallback으로 사용하지 않는다.
지원 Host 계약이 달라졌다면 pinned upstream commit과 capability 표를 먼저 갱신한다.

#### G-3 — 실제 provider 비교

- 같은 prompt·설정·Host lifecycle에서 최소 두 개 이상의 실제 provider를 비교한다.
- 특정 모델명을 예외 처리하지 않고 streaming shape, reasoning 길이와 응답 지연이
  finality 의미를 바꾸지 않는지 확인한다.
- provider 호출은 사용자 자격 증명·비용·외부 전송이 필요한 실험이므로 실행 전에
  대상 provider와 비용 범위를 명시한다.

#### G-4 — Windows 신규 설치와 기존 설치 업데이트

- 빈 사용자 data root에서 관리형 MariaDB·Python·Chroma 설치와 재기동
- Python만 있음, Python은 있으나 chromadb 없음, 잘못된 Chroma 버전
- 3.5 기존 설치의 DB·Vector data 보존 upgrade
- `127.0.0.1`·`localhost` MariaDB CRUD·DDL
- packaged launcher의 Chroma endpoint·upsert·exact readback·cleanup
- compatibility bridge 뒤 `/ready`·`/version` 확인과 health commit
- health 실패·중단·tamper에서 byte-for-byte rollback과 data root 보존

#### G-5 — 품질·지연·비용 측정

동일 fixture와 설정으로 다음을 raw evidence와 함께 기록한다.

- 첫 출력 candidate 중 accepted final로 채택된 비율
- pending confirmation 발생률과 해소 신호
- reroll·delete·edit 후 stale 결과 폐기율과 잘못된 저장 0건
- main response, Host final 확인, raw save, Critic, derived, Vector 단계별 지연
- main·Supervisor·Critic 호출 수, token 또는 provider가 제공하는 비용
- 최종 기억 eligible/selected/delivered/deferred/dropped 수와 품질 표본

#### G 종료 조건

- source commit과 loaded artifact hash가 하나의 evidence manifest로 연결됨
- G-1~G-5의 필수 matrix에 `passed`, `failed`, `blocked`, `not_applicable` 중 하나가
  기록되고 미기록 항목이 없음
- release-blocking 실패가 0
- 실제 사용자 `.env`, DB, Chroma persist data와 credential이 package에 없음
- 실패·blocked 항목을 source test 성공으로 대체하지 않음

### P1 — 세션·OS·업데이트

1. 46개 직접 + 4개 간접 session table manifest: 작성 완료, 전체 executor/parity 미완료
2. backend durable host-chat binding: source 구현·회귀 완료, loaded RisuAI 미검증
3. pin 실패 전파와 locked-source redirect: source 구현·회귀 완료
4. Windows Chroma source-live roundtrip: 완료, packaged full-local upgrade 미검증
5. 현재 로컬 기존 MariaDB privilege live gate: 완료
6. 새 updater transaction/recovery: 완료
7. 3.0.0/3.0.1/3.5.0→3.7 authenticated compatibility bridge: package E2E 완료,
   loaded launcher health commit/rollback 미검증

### P2 — UI 사실성

1. eligible/selected/delivered/deferred/dropped typed fact: source 구현·회귀 완료
2. host/backend 처리와 raw/derived/vector lane 분리: source 구현·회귀 완료
3. OOC·삭제·리롤·중복 notice/warning/error 분리: source 구현·회귀 완료
4. normal/notice/warning/error와 backend dismissal policy: source 구현·회귀 완료
5. host/backend turn 비교 화면: source 구현·회귀 완료
6. current request queue와 과거/다른 session queue 분리: source 구현·회귀 완료
7. exact request 선택·operation idempotency·maintenance provenance 분리:
   source 구현·회귀 완료
8. actual RisuAI/PocketRisu severity·dismiss·reroll lifecycle: loaded artifact 미검증

## 21. 필수 검증 행렬

### 21.1 RisuAI lifecycle

- non-streaming 정상 응답
- streaming 정상 응답
- 긴 reasoning
- 응답 후 inlay/번역 플러그인
- DeepSeek/GLM 각 1회 이상
- reroll
- 마지막 assistant 삭제
- `/cut`
- chat copy/fork
- character order 변경
- 모바일 PocketRisu/remote browser
- plugin unload/reload

각 테스트에서 candidate, accepted final, saved raw가 정확히 한 번이어야 한다.

### 21.2 queue와 idempotency

- pending confirmation은 attempts 0
- transport 실패만 attempts 증가
- max attempts 도달 후 terminal
- stale revision은 즉시 discard/supersede
- backend processing은 중복 POST하지 않음
- completed success는 queue 제거
- reload 후 상태 복구

### 21.3 critic

- provider 401/429/500
- timeout
- empty content
- valid JSON
- fenced JSON
- truncated JSON
- invalid JSON
- schema-invalid JSON
- same turn retry
- reroll 중 늦게 도착한 결과

### 21.4 MariaDB/Chroma

- 신규 설치
- 3.5 기존 설치 upgrade
- localhost/127.0.0.1 권한
- Chroma runtime 없음
- Python은 있으나 chromadb 없음
- 잘못된 Chroma 버전
- Chroma 기동 성공 후 Go endpoint 미설정
- upsert/readback
- Chroma 실패 후 outbox retry

### 21.5 migration

- 46개 직접 session table sentinel
- 간접 child sentinel
- copy/count/hash/FK parity
- concurrent write fence
- vector expected ID parity
- Chroma 성공 후 MariaDB 실패
- MariaDB 성공 후 Chroma 실패
- cleanup retry
- rollback retry
- character index 이동 후 reload
- manual attach 우선순위

## 22. 3.7 배포 차단 조건

다음이 하나라도 남으면 3.7 release gate는 열지 않는다.

- session cleanup이 전체 manifest parity 없이 실행 가능
- hidden fixed times가 finality 의미를 결정
- pending confirmation이 실패 큐에 들어감
- 무한 attempts 경로 존재
- failed `complete_turn` queue가 idempotency/generation이 다른 항목을 같은
  session/turn key로 합치거나 overflow 항목을 상태 없이 제거
- logical replacement 하위 오류 미분류
- critic upstream과 parse 오류가 같은 코드
- packaged `full_local` Windows에서 관리형 Chroma endpoint/upsert/readback 미검증
- packaged `full_local` Windows에서 기존 설치 MariaDB CRUD/DDL 권한 gate 미검증
- updater bridge 이후 loaded launcher health commit/rollback 미검증
- 실제 RisuAI reroll/delete/streaming lifecycle 미검증
- package 기본 버전과 plugin·backend 버전 불일치
- source commit → package manifest → loaded artifact hash 계보 미검증
- session migration 제공 범위가 확정되지 않은 채 부분 executor를 사용 가능으로 표시
- legacy dashboard runtime-state 문자열 판정을 3.7 지원 범위로 남겨 두면서 typed
  fact 전환 또는 명시적 범위 제외가 확정되지 않음

## 23. 사용자 지원 답변 기준

### `waiting for RisuAI active chat confirmation`

> 시간 감시를 계속한다는 뜻이 아닙니다. 공식 RisuAI post-commit hook이 없어
> 현재 후보를 아직 canonical 저장으로 확정하지 않은 상태이며, 다음
> `input`/`beforeRequest`에서 active chat의 직전 assistant를 확인해 backfill합니다.
> 이 상태는 실패 큐 attempts를 소비하지 않습니다. 다음 host signal 뒤에도
> 유지되면 request ID, session ID, user anchor, active chat role/index와
> source revision을 확인합니다.

### `raw:ok derived:ok verify_unavailable`

> 원문과 파생 자료 저장은 성공했습니다. 마지막 문구는 선택적 read-back을 시간 안에
> 확인하지 못했다는 뜻이며 저장 실패와 다릅니다.

### `CRITIC_LLM_FAILED`

> 3.7-C source에서는 공급자 호출·timeout·HTTP 오류·빈 응답·JSON 누락·문법 오류와
> schema 오류를 별도 `CRITIC_*` 코드로 표시합니다. `stage`, `retryable`,
> 가능한 경우 `http_status`와 비밀값이 제거된 `raw_preview`를 함께 확인합니다.
> 배포된 plugin/backend가 3.7-C 이전이면 여전히 `CRITIC_LLM_FAILED` 하나로 보일 수
> 있으므로 loaded artifact의 build ID를 먼저 확인합니다. 원문 저장 성공 여부는
> 계속 별도로 봅니다.

### `vector_not_configured`

> 과거 세션의 오류가 아니라 현재 실행 중인 backend에 Chroma endpoint가 연결되지
> 않았다는 서버 공통 상태입니다. MariaDB raw/derived 저장과 Vector upsert는 별개입니다.

### `LOGICAL_TURN_REPLACE_FAILED`

> 3.7-B source에서는 `code`, `stage`, `retryable`, `commit_state`,
> `raw_committed`로 하위 오류를 구분합니다. 권한·revision·중복·constraint·lock
> wait/deadlock·commit 결과 불명과 post-commit reconciliation 실패에 따라
> 재시도 여부가 달라집니다. 배포본이 구형 generic 코드만 반환하면 반복 재시도 전에
> HTTP 응답과 backend latest tail을 확인합니다.

## 24. 검증 상태와 변경 경계

이 문서와 연결된 3.7-A~F 작업에서 수행한 것은 다음이다.

- 기준 커밋과 clean worktree 확인
- `Archive Center.js` finality, queue, session pin, verify, HUD, updater 경로 재검사
- Go complete-turn, critic parser, provider JSON, protected memory, budget,
  maintenance, reindex, logical replacement 경로 재검사
- MariaDB session migration copy/count/cleanup 및 session listing 재검사
- Windows managed Chroma/MariaDB/updater source 재검사
- RisuAI 공식 API v3 pinned commit과 open post-commit listener PR 재검사
- 기존 병렬 migration·critic/memory 감사 결과와 현재 소스 대조
- 3.7-A migration 기준선·위험 차단과 fail-closed source cleanup
- 3.7-B RisuAI host final 관측, pending confirmation, 실패 큐·idempotency·typed
  replacement 정리
- 3.7-C Critic typed failure, protected quarantine, revision-safe worker·identity
  writer와 maintenance audit 분리
- 3.7-D 전체 session manifest와 fail-closed parity ledger, stable host binding,
  Windows DB/Vector live probe, updater transaction/recovery와 migration inventory
  계약 구현
- 3.7-D 재검증에서 target mutation 전 manifest 차단, 필수 Vector readiness,
  Windows launcher의 DB/Vector live probe 실행 연결 보완
- 3.7-E typed workflow fact, severity/dismissal, Host/backend turn alignment,
  OOC·삭제·리롤·중복 알림과 current/historical queue 분리 구현
- 3.7-F topK·핵심 기억 K 분리, relevance 기반 Persona·hierarchy·주관 기억,
  Supervisor 오류·강도 계약과 HUD 기억 lineage 연결 구현

변경 경계:

- 3.7-A 기준 커밋: `da6ec59`
- 3.7-B 기준 커밋: `71fcde3`
- 3.7-C 기준 커밋: `b779f25`
- 3.7-D 기준 커밋: `4756289`, 재검증 보완 `8f5b86f`
- 3.7-E 기준 커밋: `68e3b35`
- 3.7-F 기준 커밋: `94c1c20`
- `Archive Center.js`: 3.7-B에서 host final 관측·pending/failed queue·HUD 전송
  adapter를 교정했고, 3.7-C에서는 기존 UI 재시도 설정을 Go로 전달하는
  transport 한 줄만 추가했다. 3.7-E에서는 typed Go ViewModel 렌더링,
  OOC observation transport와 queue correlation metadata만 추가했다.
- Go runtime: 3.7-A의 migration guard, 3.7-B의 source/idempotency/queue owner,
  3.7-C의 Critic·revision·maintenance owner, 3.7-E의 HUD/dashboard ViewModel
  owner, 3.7-F의 기억 선택·최종 K·Supervisor 분류 owner를 수정
- DB schema: 3.7-C에서 새 table이나 column을 추가하지 않았고 기존 active source
  transaction fence를 사용
- 사용자 `.env`: 접근·수정 없음
- 3.7-D source-live probe는 현재 로컬 MariaDB에 additive schema를 적용하고
  임시 권한 table을 생성·삭제했다. 기존 사용자 row는 삭제하지 않았다.
- Chroma에는 고유 임시 collection만 생성했고 readback 뒤 삭제를 확인했다.

자동 회귀와 3.7-D source-driven live probe는 현재 로컬 MariaDB/ChromaDB
roundtrip까지 증명했다. 그러나 loaded plugin, packaged full-local launcher,
실제 provider와 RisuAI host lifecycle을 대신하지 않는다. 남은 release gate는
위 범위를 별도 artifact와 실제 host에서 검증해야 한다.

2026-07-30에 `94c1c20` 이후의 pre-G B/D/E source closure 변경셋을 다시 검증했다.

- bundled Node로 `Archive Center.js --check`: 통과
- `ARCHIVE_CENTER_NODE_BINARY`를 bundled Node로 지정한 `go test ./...`: 통과
- 이 문서와 통합 로드맵의 상대 Markdown link 검사: 통과
- `git diff --check`: 통과

첫 Go 실행에서 시스템 `node`가 PATH에 없어 `js-route-variant-smoke` 3건이 실행되지
않았으나, 테스트 계약의 `ARCHIVE_CENTER_NODE_BINARY`를 지정한 재실행에서는 전체
package가 통과했다. 이는 코드 결함이 아니라 검증 환경의 실행 파일 탐색 문제이며,
loaded artifact 또는 실제 RisuAI 검증으로 계산하지 않는다.

전체 자동 회귀 통과가 loaded artifact 성공을 뜻하지는 않는다. 3.7-B/D/E의
pre-G source 차단 항목은 닫혔지만, 실제 pluginStorage 장애, RisuAI lifecycle,
실 MariaDB·Chroma 동시성·복구와 managed single-process 밖의 외부 Vector 변경은
3.7-G 실환경 증거가 필요하다.

### 2026-07-30 Windows 테스트 빌드 전 최종 source closure

테스트 빌드 생성 직전 다음 누락 경로를 추가로 닫았다.

- PocketRisu 공식 소스 `63832a138c14cc7f11364cf7efdcb61950e7894c`의 reroll 및
  swipe 동작을 기준으로 host 관측을 교정했다. 새 message ID는 reroll, 같은
  message ID에서 `swipeId`와 본문이 바뀐 경우도 reroll, 같은 message ID와
  `swipeId`에서 본문만 바뀐 경우는 edit로 분류한다. host가 제공하지 않은 branch
  ID는 만들지 않는다.
- `candidate -> accepted final -> raw save` 이후 파생 저장 재조정에 쓰는
  idempotency key는 JavaScript가 시간값으로 만들지 않고 Go가 발급한
  `reconciliation_retry_idempotency_key`만 사용한다. key가 없는 경우 원래
  complete-turn key를 재사용하지 않고 `reconciliation_retry_key_missing`으로
  중단한다.
- HUD status stream이 non-terminal EOF로 끝나도 JavaScript가 즉시 무제한
  재연결하지 않는다. HUD-only stream 손실은 턴·저장 실패 증거가 아니므로
  붉은 `처리 오류`나 `stream_transport dropped` fact로 표시하지 않고 stale
  진행 카드만 제거하며 typed reason은 debug log에 남긴다. 실제 prepare-turn 또는
  complete-turn backend transport 실패만 해당 단계의 오류로 표시한다.
- session normalize의 `raw_repair_replay`는 실제 후보 수와 stage별
  `display_total`을 Go에서 발행한다. UI는 요청 payload의 예상 개수를 진행률로
  재사용하지 않는다. 취소 context는 막힌 chat-log 조회·저장까지 전달된다.
- Recomposer의 별도 12초 context timeout, 30초 composer reserve, 5초 reserve
  guard, 3개 endpoint 직렬화 기준과 abort 뒤 300/500ms 대기를 제거했다. 남은
  provider timeout과 전체 pipeline deadline은 설정 UI의 역할별 timeout 및
  preset에서 유도된다.
- reference source admission에서 두 번째 reconciliation이 독립 근거 둘을 primary
  하나로 축소하던 경로를 수정했다. 기존 `evidence_set`을 유지하고 비어 있을 때만
  primary로 보완한다.

빌드 전 자동 검증 결과는 다음과 같다.

- `Archive Center.js`, `Risu Recomposer.js` Node syntax: 통과
- Recomposer in-memory/batch1/input-enhance: `26/26`, `15/15`, `22/22`
- `go test ./... -count=1 -timeout 20m`: 전체 통과
- `go vet ./...`: 통과
- PowerShell parser: `8/8`
- `git diff --check`: 통과

위 결과에는 summary, entity, relationship, direct evidence, world rule, Critic,
session normalize, reroll source acceptance, reference source admission 및 updater
회귀가 포함된다. 다만 실제 PocketRisu/RisuAI에 로드한 reroll·delete·streaming,
실사용자 기존 MariaDB의 권한 편차, 최초 Windows 관리형 Chroma 다운로드 및 실제
provider 호출은 테스트 빌드에서 확인해야 할 3.7-G 실환경 gate로 남긴다.

### 2026-07-30 Windows 실행기 3.5 흐름 복원과 Chroma 필수화

3.7 테스트 실행기에서 새로 추가했던
`AC_EXTERNAL_OPERATION_TIMEOUT_SECONDS` 등의 필수 입력 계약은 기본
`.env.full.local`과 충돌해 최초 Chroma 설치 전에 실행을 중단시켰다. 이 계약과
별도 bounded child-process wrapper는 제거했다.

- MariaDB·Python·Chroma 설치와 updater child-process 실행은 3.5 관리형
  Windows 실행기처럼 별도 timeout 입력 없이 종료까지 기다린다.
- MariaDB와 Chroma 준비 확인은 3.0/3.5의 기존 `Wait-Port` 흐름을 사용한다.
- `full_local` Windows 백엔드는 `off` 또는 `fallback` vector mode로 시작할 수
  없다. 기본은 `bundled`이며, external mode를 선택해도 실제 Chroma endpoint가
  필요하다.
- Chroma runtime이 없거나 버전이 다르면 관리형 Python/Chroma를 설치·복구한다.
  관리형 Chroma endpoint 준비와 backend vector mode는 필수로 유지하지만,
  launcher가 매 시작마다 임시 collection을 만들고 upsert/readback/delete하는
  상시 probe는 실행하지 않는다.
- 접근이 거부된 기존 시스템 Python은 호환 bootstrap 후보에서 제외하고 다음
  후보 또는 검증된 관리형 Python 설치로 진행한다.
- 고정 테스트 폴더는 새 ZIP 없이 같은 경로에 갱신하며
  `.env.full.local`은 덮어쓰거나 삭제하지 않는다.

검증 결과:

- 관련 PowerShell 4개 parser: 통과
- managed Windows package build manifest: `green`
- 고정 테스트 폴더의 source/staging installer hash 일치 및 local env hash 보존:
  통과
- 실제 고정 폴더 실행에서 기존 접근 불가 Python 후보를 건너뛰고 공식 Python
  다운로드 단계까지 진입: 확인
- 현재 Codex 실행 환경은 외부 `python.org` 연결이 차단되어 최초 다운로드,
  Chroma 기동, endpoint 왕복, Go backend 시작은 이 환경에서 완료하지 못했다.
  따라서 이 마지막 항목은 사용자 네트워크 환경의 3.7-G 실환경 gate로 남는다.

### 2026-07-31 PocketRisu 리롤 출력 반환 지연 교정

실사용에서 PocketRisu 리롤을 실행했을 때 새 본문이 화면에 반영되지 않고 HUD가
`6/12 본문 응답 기다리는 중`에 머무르는 현상을 재현했다. 원인은 공식
`afterRequest` handler가 응답 문자열을 RisuAI에 반환하기 전에 active chat과
backend session routing을 `await`하던 경로였다. 모델 응답은 이미 도착했지만 이
조회가 끝날 때까지 host replacer 반환 자체가 보류될 수 있었다.

- `onAfterRequest`를 동기 host adapter로 복원했다. 표시할 응답은 hook 안에서 즉시
  반환하고, raw·derived 저장은 반환 뒤 Promise microtask에서 시작한다.
- session 좌표는 같은 요청의 `beforeRequest`에서 이미 확정해 둔
  `_chatSessionId`를 재사용한다. 표시 경로에서는 active chat 재조회나 backend
  session canonicalization을 실행하지 않는다.
- 새 watcher, polling, 고정 대기 시간, synthetic `afterRequest`는 추가하지 않았다.
  다음 `input`/`beforeRequest` active-chat backfill은 host signal 기반 복구 경로로만
  남긴다.
- Go의 request correlation·source revision·logical replacement가 canonical
  reroll 결정과 정확히 한 번 저장을 계속 소유한다. JavaScript는 공식 hook 관측,
  좌표 전달, 응답 반환만 담당한다.
- 운영 `onAfterRequest` 함수를 그대로 추출한 회귀 fixture에서 session resolver가
  영원히 끝나지 않는 조건을 주어도 PocketRisu 리롤 본문이 Promise가 아닌 문자열로
  즉시 반환되고, 저장 예약은 한 번만 일어나며 중복 callback은 두 번째 저장을
  예약하지 않는지 검증한다.

공식 기준은 RisuAI plugin text replacer의 `afterRequest` 계약과 pinned upstream
commit `846c897bf56e3e0121ef12e7ba07f5c3912dfa78`이며, 2026-07-31에 다시 대조했다.
source 회귀는 host 반환과 backend replacement 계약을 검증하지만, 실제
PocketRisu에 로드된 artifact에서 본문 교체와 HUD 종료를 확인하는 절차는
3.7-G 실환경 검증으로 남는다.

### 2026-07-31 완료 HUD 상세 정보 분리

3.5의 간결한 결과 카드에 가깝게 플로팅 HUD의 terminal 화면을 정리했다.
완료 화면에는 턴·저장 상태, 생성·저장 항목별 숫자, 사용자용 경고, 전체 단계와
소요 시간만 표시한다.

`memory_selection` 요약, 레인별 전달 수, 최종 기억 본문과 제외 사유,
typed workflow fact 표는 backend ViewModel에서 삭제하지 않고 terminal HUD에서만
제외했다. 이 자료는 대시보드와 디버그의 입력 조립·전달 lineage·workflow
inspection에서 계속 확인할 수 있다. 실패·중단 HUD의 오류 코드, 실패 단계,
보존된 생성 수와 단계별 원인은 숨기지 않는다.

### 2026-07-31 3.7 기능 기준선 고정과 실사용 확인

3.8 작업이 3.7 안정화 결과를 다시 바꾸지 않도록 다음 상태를 3.7의 고정 기준선으로
기록한다.

- source commit: `3315bbe269f0fba5afb16f1dce8ad44be459db3c`
- Windows 고정 테스트 폴더 build:
  `3.7-hud-success-compact.20260731-1`
- 사용자가 실제 테스트 빌드에서 확인한 항목:
  - PocketRisu/RisuAI 리롤 뒤 새 출력의 즉시 인식과 저장
  - 표시 턴 삭제의 감지와 후속 처리
  - 현재 Host 턴과 Archive Center 턴 인식
  - 기존 대화 콜드 스타트/세션 정상화
  - 진행·완료·오류 HUD 표시와 완료 카드 닫기
- 완료 HUD는 요약 숫자와 단계·시간만 표시하고, 기억 선택·레인·제외 사유·typed
  workflow 상세는 대시보드와 디버그에 유지한다.
- 오류·경고 HUD의 X 버튼 전 유지 정책은 3.7 기준 동작으로 보존한다.

이 확인으로 3.7의 사용자 핵심 수명주기와 HUD는 다음 버전의 회귀 기준선이 된다.
3.8은 이 동작을 재설계하지 않고 Go의 정밀 기억 계약만 확장한다.

다만 이 기록은 사용자가 확인한 위 항목의 live evidence다. 3.7-G에 정의된 모든
provider 비교, Windows 신규 설치·3.5 upgrade, MariaDB·Chroma fault/recovery와 장기
성능·비용 행렬까지 완료했다는 뜻은 아니다. 그 항목은 3.8 기능 구현을 되돌리는
조건이 아니라 별도의 배포 증거 보강 목록으로 유지한다.
